// @ts-nocheck
// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs
// Source chapter: agents_self_contained/s_full.ts
// Tree-shaken bundle: chapter wiring + only used runtime code.
// agents_self_contained/_runtime.ts
import Anthropic from "@anthropic-ai/sdk";
import dotenv from "dotenv";
import { execSync, spawn, spawnSync } from "node:child_process";
import fs from "node:fs";
import fsp from "node:fs/promises";
import path from "node:path";
import process2 from "node:process";
import readline from "node:readline/promises";
import { stdin as input, stdout as output } from "node:process";
dotenv.config({ override: true });
var WORKDIR = process2.cwd();
var DEFAULT_MODEL = "claude-3-5-sonnet-latest";
var anthropicClient = null;
function getModelId() {
  return process2.env.MODEL_ID || DEFAULT_MODEL;
}
function getAnthropicClient() {
  if (anthropicClient) {
    return anthropicClient;
  }
  anthropicClient = new Anthropic({
    apiKey: process2.env.ANTHROPIC_API_KEY || process2.env.ANTHROPIC_AUTH_TOKEN || "missing-api-key",
    baseURL: process2.env.ANTHROPIC_BASE_URL || void 0
  });
  return anthropicClient;
}
function createLoopContext() {
  return { workdir: WORKDIR, messages: [], meta: {} };
}
function safePath(relativePath) {
  const resolved = path.resolve(WORKDIR, relativePath);
  const relative = path.relative(WORKDIR, resolved);
  if (relative.startsWith("..") || path.isAbsolute(relative)) {
    throw new Error(`Path escapes workspace: ${relativePath}`);
  }
  return resolved;
}
function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {
  const dangerous = ["rm -rf /", "sudo ", "shutdown", "reboot", "> /dev/"];
  if (dangerous.some((item) => command.includes(item))) {
    return "Error: Dangerous command blocked";
  }
  try {
    const output2 = execSync(command, {
      cwd,
      encoding: "utf8",
      timeout: timeoutMs,
      maxBuffer: 10 * 1024 * 1024,
      shell: "/bin/zsh"
    });
    return output2.trim() || "(no output)";
  } catch (error) {
    const stdout = typeof error === "object" && error !== null && "stdout" in error ? String(error.stdout || "") : "";
    const stderr = typeof error === "object" && error !== null && "stderr" in error ? String(error.stderr || "") : "";
    const message = `${stdout}${stderr}`.trim();
    if (message) {
      return message.slice(0, 5e4);
    }
    if (error instanceof Error) {
      return `Error: ${error.message}`;
    }
    return "Error: Unknown shell failure";
  }
}
function readWorkspaceFile(filePath, limit) {
  try {
    const text = fs.readFileSync(safePath(filePath), "utf8");
    const lines = text.split(/\r?\n/u);
    if (typeof limit === "number" && limit > 0 && lines.length > limit) {
      return `${lines.slice(0, limit).join("\n")}
... (${lines.length - limit} more lines)`;
    }
    return text.slice(0, 5e4);
  } catch (error) {
    return `Error: ${error instanceof Error ? error.message : String(error)}`;
  }
}
function writeWorkspaceFile(filePath, content) {
  try {
    const resolved = safePath(filePath);
    fs.mkdirSync(path.dirname(resolved), { recursive: true });
    fs.writeFileSync(resolved, content, "utf8");
    return `Wrote ${Buffer.byteLength(content, "utf8")} bytes to ${filePath}`;
  } catch (error) {
    return `Error: ${error instanceof Error ? error.message : String(error)}`;
  }
}
function editWorkspaceFile(filePath, oldText, newText) {
  try {
    const resolved = safePath(filePath);
    const content = fs.readFileSync(resolved, "utf8");
    const index = content.indexOf(oldText);
    if (index === -1) {
      return `Error: Text not found in ${filePath}`;
    }
    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;
    fs.writeFileSync(resolved, updated, "utf8");
    return `Edited ${filePath}`;
  } catch (error) {
    return `Error: ${error instanceof Error ? error.message : String(error)}`;
  }
}
function extractText(content) {
  if (typeof content === "string") {
    return content;
  }
  return content.map((block) => {
    if (typeof block === "object" && block !== null && "text" in block && typeof block.text === "string") {
      return block.text;
    }
    if (typeof block === "object" && block !== null && "content" in block && typeof block.content === "string") {
      return block.content;
    }
    return "";
  }).filter(Boolean).join("\n").trim();
}
function normalizeContent(content) {
  if (typeof content === "string") {
    return content;
  }
  return content.filter((block) => typeof block === "object" && block !== null).map((block) => {
    const cleaned = Object.fromEntries(
      Object.entries(block).filter(([key]) => !key.startsWith("_"))
    );
    return cleaned;
  });
}
function normalizeMessages(messages) {
  const cleaned = messages.map((message) => ({
    role: message.role,
    content: normalizeContent(message.content)
  }));
  const existingToolResults = /* @__PURE__ */ new Set();
  for (const message of cleaned) {
    if (!Array.isArray(message.content)) {
      continue;
    }
    for (const block of message.content) {
      if (typeof block === "object" && block !== null && block.type === "tool_result" && typeof block.tool_use_id === "string") {
        existingToolResults.add(block.tool_use_id);
      }
    }
  }
  for (const message of cleaned) {
    if (message.role !== "assistant" || !Array.isArray(message.content)) {
      continue;
    }
    for (const block of message.content) {
      if (typeof block === "object" && block !== null && block.type === "tool_use" && typeof block.id === "string" && !existingToolResults.has(block.id)) {
        cleaned.push({
          role: "user",
          content: [{ type: "tool_result", tool_use_id: block.id, content: "(cancelled)" }]
        });
      }
    }
  }
  const merged = [];
  for (const message of cleaned) {
    const previous = merged.at(-1);
    if (!previous || previous.role !== message.role) {
      merged.push(message);
      continue;
    }
    const previousBlocks = typeof previous.content === "string" ? [{ type: "text", text: previous.content }] : previous.content;
    const nextBlocks = typeof message.content === "string" ? [{ type: "text", text: message.content }] : message.content;
    previous.content = [...previousBlocks, ...nextBlocks];
  }
  return merged;
}
async function callModel(systemPrompt, messages, tools2) {
  const client = getAnthropicClient();
  return client.messages.create({
    model: getModelId(),
    system: systemPrompt,
    messages,
    tools: tools2,
    max_tokens: 8e3
  });
}
async function runAgentLoop(ctx2, config) {
  const maxTurns = config.maxTurns ?? 30;
  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));
  for (let turn = 0; turn < maxTurns; turn += 1) {
    if (config.beforeModel) {
      await config.beforeModel(ctx2);
    }
    const systemPrompt = typeof config.systemPrompt === "function" ? config.systemPrompt() : config.systemPrompt;
    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;
    let response;
    let modelAttempt = 0;
    for (; ; ) {
      try {
        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;
        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));
        break;
      } catch (error) {
        modelAttempt += 1;
        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;
        if (!shouldRetry) {
          throw error;
        }
      }
    }
    ctx2.messages.push({ role: "assistant", content: response.content });
    if (response.stop_reason !== "tool_use") {
      return;
    }
    const toolResults = [];
    for (const block of response.content) {
      if (block.type !== "tool_use") {
        continue;
      }
      const registered = toolMap.get(block.name);
      const toolUseId = String(block.id || cryptoRandomId());
      const input2 = typeof block.input === "object" && block.input !== null ? block.input : {};
      let outputText;
      if (!registered) {
        outputText = `Unknown tool: ${block.name}`;
      } else {
        outputText = await registered.execute(input2, ctx2, toolUseId);
      }
      console.log(`> ${block.name}`);
      console.log(outputText.slice(0, 200));
      toolResults.push({ type: "tool_result", tool_use_id: toolUseId, content: outputText });
    }
    ctx2.messages.push({ role: "user", content: toolResults });
    if (config.afterToolResults) {
      await config.afterToolResults(ctx2);
    }
  }
}
async function runRepl(options) {
  const rl = readline.createInterface({ input, output });
  try {
    if (options.onSessionStart) {
      await options.onSessionStart(options.ctx);
    }
    for (; ; ) {
      const query = await rl.question(`\x1B[36m${options.label} >> \x1B[0m`);
      if (!query.trim() || ["q", "exit"].includes(query.trim().toLowerCase())) {
        break;
      }
      options.ctx.messages.push({ role: "user", content: query });
      await options.run(options.ctx);
      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === "assistant");
      const finalText = lastAssistant ? extractText(lastAssistant.content) : "";
      if (finalText) {
        console.log(finalText);
      }
      console.log("");
    }
  } finally {
    rl.close();
  }
}
function cryptoRandomId() {
  return Math.random().toString(36).slice(2, 10);
}
function withGuards(toolName, input2, action, options) {
  const hookContext = { tool_name: toolName, tool_input: input2 };
  if (options?.permissions) {
    const decision = options.permissions.check(toolName, input2);
    if (decision.behavior === "deny") {
      return `Permission denied: ${decision.reason}`;
    }
    if (decision.behavior === "ask" && !options.permissions.askUser(toolName, input2)) {
      return `Permission denied by user for ${toolName}`;
    }
  }
  if (options?.hooks) {
    const pre = options.hooks.runHooks("PreToolUse", hookContext);
    if (pre.blocked) {
      return `Blocked by hook: ${pre.blockReason || "unknown"}`;
    }
  }
  let outputText = action();
  if (toolName === "read_file" && typeof input2.path === "string" && options?.onReadPath) {
    options.onReadPath(input2.path);
  }
  if (options?.compact) {
    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);
  }
  if (options?.hooks) {
    const post = options.hooks.runHooks("PostToolUse", { ...hookContext, tool_output: outputText });
    if (post.messages.length > 0) {
      outputText = `${outputText}

${post.messages.join("\n")}`;
    }
  }
  return outputText.slice(0, 5e4);
}
function createBaseTools(options) {
  return [
    {
      spec: {
        name: "bash",
        description: "Run a shell command in the current workspace.",
        input_schema: {
          type: "object",
          properties: { command: { type: "string" } },
          required: ["command"]
        }
      },
      execute: (input2, _ctx, toolUseId) => withGuards(
        "bash",
        input2,
        () => runBash(String(input2.command || "")),
        { ...options, toolUseId }
      )
    },
    {
      spec: {
        name: "read_file",
        description: "Read file contents from the workspace.",
        input_schema: {
          type: "object",
          properties: {
            path: { type: "string" },
            limit: { type: "integer" }
          },
          required: ["path"]
        }
      },
      execute: (input2, _ctx, toolUseId) => withGuards(
        "read_file",
        input2,
        () => readWorkspaceFile(String(input2.path || ""), typeof input2.limit === "number" ? input2.limit : void 0),
        {
          ...options,
          toolUseId,
          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)
        }
      )
    },
    {
      spec: {
        name: "write_file",
        description: "Write a file in the workspace.",
        input_schema: {
          type: "object",
          properties: {
            path: { type: "string" },
            content: { type: "string" }
          },
          required: ["path", "content"]
        }
      },
      execute: (input2, _ctx, toolUseId) => withGuards(
        "write_file",
        input2,
        () => writeWorkspaceFile(String(input2.path || ""), String(input2.content || "")),
        { ...options, toolUseId }
      )
    },
    {
      spec: {
        name: "edit_file",
        description: "Replace exact text in a file once.",
        input_schema: {
          type: "object",
          properties: {
            path: { type: "string" },
            old_text: { type: "string" },
            new_text: { type: "string" }
          },
          required: ["path", "old_text", "new_text"]
        }
      },
      execute: (input2, _ctx, toolUseId) => withGuards(
        "edit_file",
        input2,
        () => editWorkspaceFile(
          String(input2.path || ""),
          String(input2.old_text || ""),
          String(input2.new_text || "")
        ),
        { ...options, toolUseId }
      )
    }
  ];
}
var TodoManager = class {
  items = [];
  roundsSinceUpdate = 0;
  update(items) {
    if (items.length > 12) {
      throw new Error("Keep the session plan short (max 12 items)");
    }
    const normalized = items.map((item, index) => {
      const content = String(item.content || "").trim();
      const status = String(item.status || "pending");
      const activeForm = String(item.activeForm || "");
      if (!content) {
        throw new Error(`Item ${index}: content required`);
      }
      if (!["pending", "in_progress", "completed"].includes(status)) {
        throw new Error(`Item ${index}: invalid status '${status}'`);
      }
      return { content, status, activeForm };
    });
    const active = normalized.filter((item) => item.status === "in_progress");
    if (active.length > 1) {
      throw new Error("Only one plan item can be in_progress");
    }
    this.items = normalized;
    this.roundsSinceUpdate = 0;
    return this.render();
  }
  noteRoundWithoutUpdate() {
    this.roundsSinceUpdate += 1;
  }
  reminder(interval = 3) {
    if (this.items.length === 0 || this.roundsSinceUpdate < interval) {
      return null;
    }
    return "<reminder>Refresh your current plan before continuing.</reminder>";
  }
  render() {
    if (this.items.length === 0) {
      return "No session plan yet.";
    }
    const lines = this.items.map((item) => {
      const marker = item.status === "completed" ? "[x]" : item.status === "in_progress" ? "[>]" : "[ ]";
      return item.status === "in_progress" && item.activeForm ? `${marker} ${item.content} (${item.activeForm})` : `${marker} ${item.content}`;
    });
    const completed = this.items.filter((item) => item.status === "completed").length;
    lines.push(`
(${completed}/${this.items.length} completed)`);
    return lines.join("\n");
  }
};
function createTodoTool(todoManager2) {
  return {
    spec: {
      name: "todo",
      description: "Rewrite the current session plan for multi-step work.",
      input_schema: {
        type: "object",
        properties: {
          items: {
            type: "array",
            items: {
              type: "object",
              properties: {
                content: { type: "string" },
                status: { type: "string", enum: ["pending", "in_progress", "completed"] },
                activeForm: { type: "string" }
              },
              required: ["content", "status"]
            }
          }
        },
        required: ["items"]
      }
    },
    execute: (input2) => {
      const items = Array.isArray(input2.items) ? input2.items : [];
      return todoManager2.update(items);
    }
  };
}
function parseFrontmatter(text) {
  const match = /^---\n([\s\S]*?)\n---\n?([\s\S]*)$/u.exec(text);
  if (!match) {
    return { meta: {}, body: text };
  }
  const meta = {};
  for (const line of match[1].split("\n")) {
    const separator = line.indexOf(":");
    if (separator === -1) {
      continue;
    }
    meta[line.slice(0, separator).trim()] = line.slice(separator + 1).trim();
  }
  return { meta, body: match[2].trim() };
}
var SkillRegistry = class {
  skillsDir;
  documents = /* @__PURE__ */ new Map();
  constructor(skillsDir = path.join(WORKDIR, "skills")) {
    this.skillsDir = skillsDir;
    this.loadAll();
  }
  loadAll() {
    this.documents.clear();
    if (!fs.existsSync(this.skillsDir)) {
      return;
    }
    const entries = walkFiles(this.skillsDir).filter((filePath) => filePath.endsWith("SKILL.md"));
    for (const filePath of entries) {
      const parsed = parseFrontmatter(fs.readFileSync(filePath, "utf8"));
      const name = parsed.meta.name || path.basename(path.dirname(filePath));
      const description = parsed.meta.description || "No description";
      this.documents.set(name, {
        name,
        description,
        path: filePath,
        body: parsed.body
      });
    }
  }
  describeAvailable() {
    if (this.documents.size === 0) {
      return "(no skills available)";
    }
    return [...this.documents.values()].sort((a, b) => a.name.localeCompare(b.name)).map((skill) => `- ${skill.name}: ${skill.description}`).join("\n");
  }
  loadFullText(name) {
    const skill = this.documents.get(name);
    if (!skill) {
      const known = [...this.documents.keys()].sort().join(", ") || "(none)";
      return `Error: Unknown skill '${name}'. Available skills: ${known}`;
    }
    const relPath = path.relative(WORKDIR, skill.path);
    return `<skill name="${skill.name}" path="${relPath}">
${skill.body}
</skill>`;
  }
};
function walkFiles(root) {
  const results = [];
  for (const entry of fs.readdirSync(root, { withFileTypes: true })) {
    const fullPath = path.join(root, entry.name);
    if (entry.isDirectory()) {
      results.push(...walkFiles(fullPath));
      continue;
    }
    results.push(fullPath);
  }
  return results;
}
function createLoadSkillTool(skillRegistry2) {
  return {
    spec: {
      name: "load_skill",
      description: "Load the full body of a named skill into the current context.",
      input_schema: {
        type: "object",
        properties: { name: { type: "string" } },
        required: ["name"]
      }
    },
    execute: (input2) => skillRegistry2.loadFullText(String(input2.name || ""))
  };
}
var CompactManager = class {
  contextLimit;
  persistThreshold;
  previewChars;
  keepRecentToolResults;
  transcriptDir;
  toolResultsDir;
  recentFiles = [];
  constructor(options) {
    this.contextLimit = options?.contextLimit ?? 5e4;
    this.persistThreshold = options?.persistThreshold ?? 3e4;
    this.previewChars = options?.previewChars ?? 2e3;
    this.keepRecentToolResults = options?.keepRecentToolResults ?? 3;
    this.transcriptDir = path.join(WORKDIR, ".transcripts");
    this.toolResultsDir = path.join(WORKDIR, ".task_outputs", "tool-results");
  }
  estimateContextSize(messages) {
    return JSON.stringify(messages).length;
  }
  trackRecentFile(filePath) {
    this.recentFiles = this.recentFiles.filter((item) => item !== filePath);
    this.recentFiles.push(filePath);
    this.recentFiles = this.recentFiles.slice(-5);
  }
  persistLargeOutput(toolUseId, output2) {
    if (output2.length <= this.persistThreshold) {
      return output2;
    }
    fs.mkdirSync(this.toolResultsDir, { recursive: true });
    const safeId = toolUseId.replace(/[^a-zA-Z0-9_.-]/gu, "_");
    const filePath = path.join(this.toolResultsDir, `${safeId}.txt`);
    if (!fs.existsSync(filePath)) {
      fs.writeFileSync(filePath, output2, "utf8");
    }
    const preview = output2.slice(0, this.previewChars);
    return [
      "<persisted-output>",
      `Full output saved to: ${path.relative(WORKDIR, filePath)}`,
      "Preview:",
      preview,
      "</persisted-output>"
    ].join("\n");
  }
  microCompact(messages) {
    const toolBlocks = [];
    for (const message of messages) {
      if (message.role !== "user" || !Array.isArray(message.content)) {
        continue;
      }
      for (const block of message.content) {
        if (typeof block === "object" && block !== null && block.type === "tool_result") {
          toolBlocks.push(block);
        }
      }
    }
    for (const block of toolBlocks.slice(0, -this.keepRecentToolResults)) {
      if (block.content.length > 120) {
        block.content = "[Earlier tool result compacted. Re-run the tool if you need full detail.]";
      }
    }
  }
  async writeTranscript(messages) {
    await fsp.mkdir(this.transcriptDir, { recursive: true });
    const filePath = path.join(this.transcriptDir, `transcript_${Date.now()}.jsonl`);
    const lines = messages.map((message) => JSON.stringify(message)).join("\n");
    await fsp.writeFile(filePath, `${lines}
`, "utf8");
    return filePath;
  }
  async compactHistory(messages, focus) {
    await this.writeTranscript(messages);
    const lastUserGoal = [...messages].reverse().find((message) => message.role === "user");
    const summaryLines = [
      "This conversation was compacted so the agent can continue working.",
      "",
      "Current goal:",
      extractText(lastUserGoal?.content || ""),
      "",
      "What happened recently:",
      ...messages.slice(-6).map((message) => `- ${message.role}: ${extractText(message.content).slice(0, 160)}`)
    ];
    if (this.recentFiles.length > 0) {
      summaryLines.push("", "Recent files to reopen:", ...this.recentFiles.map((item) => `- ${item}`));
    }
    if (focus) {
      summaryLines.push("", `Focus to preserve: ${focus}`);
    }
    return [{ role: "user", content: summaryLines.join("\n") }];
  }
};
var PermissionManager = class {
  mode;
  rules = [
    { tool: "bash", content: "rm -rf /", behavior: "deny" },
    { tool: "bash", content: "sudo ", behavior: "deny" },
    { tool: "read_file", path: "*", behavior: "allow" }
  ];
  consecutiveDenials = 0;
  constructor(mode = "default") {
    this.mode = mode;
  }
  check(toolName, toolInput) {
    if (toolName === "bash") {
      const command = String(toolInput.command || "");
      if (/(^|\s)sudo(\s|$)/u.test(command) || /\brm\s+-[a-zA-Z]*r/u.test(command)) {
        return { behavior: "deny", reason: "Bash validator flagged a high-risk command" };
      }
      if (/[;&|`]/u.test(command) || /\$\(/u.test(command)) {
        return { behavior: "ask", reason: "Bash validator flagged shell metacharacters" };
      }
    }
    for (const rule of this.rules.filter((rule2) => rule2.behavior === "deny")) {
      if (this.matches(rule, toolName, toolInput)) {
        return { behavior: "deny", reason: `Blocked by deny rule for ${toolName}` };
      }
    }
    if (this.mode === "plan") {
      return ["read_file"].includes(toolName) ? { behavior: "allow", reason: "Plan mode allows read-only tools" } : { behavior: "deny", reason: "Plan mode blocks state changes" };
    }
    if (this.mode === "auto" && ["read_file"].includes(toolName)) {
      return { behavior: "allow", reason: "Auto mode allows read-only tools" };
    }
    for (const rule of this.rules.filter((rule2) => rule2.behavior === "allow")) {
      if (this.matches(rule, toolName, toolInput)) {
        return { behavior: "allow", reason: `Matched allow rule for ${toolName}` };
      }
    }
    return { behavior: "ask", reason: `No rule matched for ${toolName}` };
  }
  askUser(toolName, toolInput) {
    const preview = JSON.stringify(toolInput).slice(0, 200);
    const result = spawnSync("/bin/zsh", ["-lc", 'printf "\\n[Permission] %s: %s\\nAllow? (y/n): " "$TOOL_NAME" "$TOOL_PREVIEW"; read answer; [[ "$answer" == "y" || "$answer" == "yes" ]]'], {
      env: {
        ...process2.env,
        TOOL_NAME: toolName,
        TOOL_PREVIEW: preview
      },
      stdio: "inherit"
    });
    return result.status === 0;
  }
  matches(rule, toolName, toolInput) {
    if (rule.tool !== "*" && rule.tool !== toolName) {
      return false;
    }
    if (rule.path && rule.path !== "*") {
      return String(toolInput.path || "").includes(rule.path.replace("*", ""));
    }
    if (rule.content) {
      return String(toolInput.command || "").includes(rule.content.replace("*", ""));
    }
    return true;
  }
};
var HookManager = class {
  hooks = {
    PreToolUse: [],
    PostToolUse: [],
    SessionStart: []
  };
  constructor(configPath = path.join(WORKDIR, ".hooks.json")) {
    if (!fs.existsSync(configPath)) {
      return;
    }
    try {
      const parsed = JSON.parse(fs.readFileSync(configPath, "utf8"));
      this.hooks = {
        PreToolUse: parsed.hooks?.PreToolUse || [],
        PostToolUse: parsed.hooks?.PostToolUse || [],
        SessionStart: parsed.hooks?.SessionStart || []
      };
    } catch (error) {
      console.error(`[Hook config error] ${error instanceof Error ? error.message : String(error)}`);
    }
  }
  runHooks(event, context) {
    const result = { blocked: false, blockReason: void 0, messages: [] };
    const trustMarker = path.join(WORKDIR, ".claude", ".claude_trusted");
    if (!fs.existsSync(trustMarker)) {
      return result;
    }
    for (const hook of this.hooks[event]) {
      if (hook.matcher && context?.tool_name && hook.matcher !== "*" && hook.matcher !== context.tool_name) {
        continue;
      }
      const env = {
        ...process2.env,
        HOOK_EVENT: event,
        HOOK_TOOL_NAME: String(context?.tool_name || ""),
        HOOK_TOOL_INPUT: JSON.stringify(context?.tool_input || {}),
        HOOK_TOOL_OUTPUT: String(context?.tool_output || "")
      };
      const executed = spawnSync("/bin/zsh", ["-lc", hook.command], {
        cwd: WORKDIR,
        env,
        encoding: "utf8",
        timeout: 3e4
      });
      if (executed.status === 1) {
        result.blocked = true;
        result.blockReason = executed.stderr?.trim() || "Blocked by hook";
      }
      if (executed.status === 2 && executed.stderr?.trim()) {
        result.messages.push(executed.stderr.trim());
      }
    }
    return result;
  }
};
var MEMORY_TYPES = ["user", "feedback", "project", "reference"];
var MemoryManager = class {
  memoryDir;
  memories = /* @__PURE__ */ new Map();
  constructor(memoryDir = path.join(WORKDIR, ".memory")) {
    this.memoryDir = memoryDir;
  }
  loadAll() {
    this.memories.clear();
    if (!fs.existsSync(this.memoryDir)) {
      return;
    }
    for (const fileName of fs.readdirSync(this.memoryDir)) {
      if (!fileName.endsWith(".md") || fileName === "MEMORY.md") {
        continue;
      }
      const parsed = parseFrontmatter(fs.readFileSync(path.join(this.memoryDir, fileName), "utf8"));
      const name = parsed.meta.name || fileName.replace(/\.md$/u, "");
      const type = parsed.meta.type || "project";
      this.memories.set(name, {
        description: parsed.meta.description || "",
        type,
        content: parsed.body,
        file: fileName
      });
    }
  }
  loadMemoryPrompt() {
    if (this.memories.size === 0) {
      return "";
    }
    const lines = ["# Memories (persistent across sessions)", ""];
    for (const memoryType of MEMORY_TYPES) {
      const entries = [...this.memories.entries()].filter(([, value]) => value.type === memoryType);
      if (entries.length === 0) {
        continue;
      }
      lines.push(`## [${memoryType}]`);
      for (const [name, value] of entries) {
        lines.push(`### ${name}: ${value.description}`);
        if (value.content.trim()) {
          lines.push(value.content.trim());
        }
        lines.push("");
      }
    }
    return lines.join("\n");
  }
  saveMemory(name, description, memoryType, content) {
    if (!MEMORY_TYPES.includes(memoryType)) {
      return `Error: type must be one of ${MEMORY_TYPES.join(", ")}`;
    }
    const safeName = name.toLowerCase().replace(/[^a-z0-9_-]/gu, "_") || "memory";
    fs.mkdirSync(this.memoryDir, { recursive: true });
    const fileName = `${safeName}.md`;
    const filePath = path.join(this.memoryDir, fileName);
    fs.writeFileSync(
      filePath,
      `---
name: ${name}
description: ${description}
type: ${memoryType}
---
${content}
`,
      "utf8"
    );
    this.memories.set(name, { description, type: memoryType, content, file: fileName });
    this.rebuildIndex();
    return `Saved memory '${name}' [${memoryType}] to ${path.relative(WORKDIR, filePath)}`;
  }
  rebuildIndex() {
    fs.mkdirSync(this.memoryDir, { recursive: true });
    const lines = ["# Memory Index", ""];
    for (const [name, value] of this.memories.entries()) {
      lines.push(`- ${name}: ${value.description} [${value.type}]`);
    }
    fs.writeFileSync(path.join(this.memoryDir, "MEMORY.md"), `${lines.join("\n")}
`, "utf8");
  }
};
function createSaveMemoryTool(memoryManager2) {
  return {
    spec: {
      name: "save_memory",
      description: "Save durable cross-session information to the memory store.",
      input_schema: {
        type: "object",
        properties: {
          name: { type: "string" },
          description: { type: "string" },
          type: { type: "string", enum: [...MEMORY_TYPES] },
          content: { type: "string" }
        },
        required: ["name", "description", "type", "content"]
      }
    },
    execute: (input2) => memoryManager2.saveMemory(
      String(input2.name || ""),
      String(input2.description || ""),
      String(input2.type || "project"),
      String(input2.content || "")
    )
  };
}
var TaskManager = class {
  taskFile;
  tasks = [];
  constructor(taskFile = path.join(WORKDIR, ".tasks", "tasks.json")) {
    this.taskFile = taskFile;
    this.load();
  }
  load() {
    if (!fs.existsSync(this.taskFile)) {
      this.tasks = [];
      return;
    }
    this.tasks = JSON.parse(fs.readFileSync(this.taskFile, "utf8"));
  }
  save() {
    fs.mkdirSync(path.dirname(this.taskFile), { recursive: true });
    fs.writeFileSync(this.taskFile, JSON.stringify(this.tasks, null, 2), "utf8");
  }
  createTask(title, notes = "") {
    const task = {
      id: cryptoRandomId(),
      title,
      status: "pending",
      notes,
      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
      updatedAt: (/* @__PURE__ */ new Date()).toISOString()
    };
    this.tasks.push(task);
    this.save();
    return `Created task ${task.id}: ${task.title}`;
  }
  listTasks() {
    if (this.tasks.length === 0) {
      return "No tasks.";
    }
    return this.tasks.map((task) => `${task.id}: [${task.status}] ${task.title}${task.owner ? ` @${task.owner}` : ""}`).join("\n");
  }
  updateTask(id, fields) {
    const task = this.tasks.find((item) => item.id === id);
    if (!task) {
      return `Error: Unknown task ${id}`;
    }
    Object.assign(task, fields);
    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
    this.save();
    return `Updated task ${id}`;
  }
  claimUnclaimed(agentName) {
    const task = this.tasks.find((item) => !item.owner && item.status !== "completed");
    if (!task) {
      return "No unclaimed tasks.";
    }
    task.owner = agentName;
    task.status = "in_progress";
    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();
    this.save();
    return `Claimed task ${task.id} for ${agentName}`;
  }
  scanUnclaimedTasks() {
    return this.tasks.filter((task) => !task.owner && task.status !== "completed");
  }
};
function createTaskBoardTools(taskManager2) {
  return [
    {
      spec: {
        name: "create_task_record",
        description: "Create a durable task-board item.",
        input_schema: {
          type: "object",
          properties: {
            title: { type: "string" },
            notes: { type: "string" }
          },
          required: ["title"]
        }
      },
      execute: (input2) => taskManager2.createTask(String(input2.title || ""), String(input2.notes || ""))
    },
    {
      spec: {
        name: "list_task_records",
        description: "List durable task-board items.",
        input_schema: { type: "object", properties: {} }
      },
      execute: () => taskManager2.listTasks()
    },
    {
      spec: {
        name: "update_task_record",
        description: "Update a task-board item.",
        input_schema: {
          type: "object",
          properties: {
            id: { type: "string" },
            status: { type: "string", enum: ["pending", "in_progress", "completed"] },
            owner: { type: "string" },
            notes: { type: "string" },
            title: { type: "string" }
          },
          required: ["id"]
        }
      },
      execute: (input2) => taskManager2.updateTask(String(input2.id || ""), {
        status: input2.status,
        owner: input2.owner ? String(input2.owner) : void 0,
        notes: input2.notes ? String(input2.notes) : void 0,
        title: input2.title ? String(input2.title) : void 0
      })
    },
    {
      spec: {
        name: "claim_unclaimed_task",
        description: "Claim the next unclaimed task for an agent.",
        input_schema: {
          type: "object",
          properties: { agent_name: { type: "string" } },
          required: ["agent_name"]
        }
      },
      execute: (input2) => taskManager2.claimUnclaimed(String(input2.agent_name || "agent"))
    }
  ];
}
var BackgroundManager = class {
  dir;
  tasks = {};
  notifications = [];
  constructor(dir = path.join(WORKDIR, ".runtime-tasks")) {
    this.dir = dir;
    fs.mkdirSync(this.dir, { recursive: true });
  }
  run(command) {
    const taskId = cryptoRandomId();
    const outputFile = path.join(this.dir, `${taskId}.log`);
    this.tasks[taskId] = {
      status: "running",
      command,
      result: null,
      startedAt: Date.now(),
      outputFile,
      resultPreview: ""
    };
    const child = spawn("/bin/zsh", ["-lc", command], {
      cwd: WORKDIR,
      stdio: ["ignore", "pipe", "pipe"]
    });
    let stdout = "";
    let stderr = "";
    child.stdout.on("data", (chunk) => {
      stdout += String(chunk);
    });
    child.stderr.on("data", (chunk) => {
      stderr += String(chunk);
    });
    child.on("close", (code) => {
      const result = `${stdout}${stderr}`.trim() || "(no output)";
      fs.writeFileSync(outputFile, result, "utf8");
      const preview = result.replace(/\s+/gu, " ").slice(0, 500);
      this.tasks[taskId] = {
        ...this.tasks[taskId],
        status: code === 0 ? "completed" : "error",
        result,
        resultPreview: preview
      };
      this.notifications.push({
        taskId,
        status: this.tasks[taskId].status,
        preview,
        outputFile: path.relative(WORKDIR, outputFile)
      });
    });
    return `Background task ${taskId} started: ${command} (output_file=${path.relative(WORKDIR, outputFile)})`;
  }
  check(taskId) {
    if (taskId) {
      const task = this.tasks[taskId];
      if (!task) {
        return `Error: Unknown task ${taskId}`;
      }
      if (task.status === "running" && !task.result) {
        return "[running] (running)";
      }
      return JSON.stringify({
        id: taskId,
        status: task.status,
        command: task.command,
        result_preview: task.resultPreview,
        output_file: path.relative(WORKDIR, task.outputFile)
      }, null, 2);
    }
    const entries = Object.entries(this.tasks);
    if (entries.length === 0) {
      return "No background tasks.";
    }
    return entries.map(([id, task]) => `${id}: [${task.status}] ${task.command} -> ${task.resultPreview || "(running)"}`).join("\n");
  }
  drainNotifications() {
    const drained = [...this.notifications];
    this.notifications = [];
    return drained;
  }
};
function createBackgroundTools(backgroundManager2) {
  return [
    {
      spec: {
        name: "background_run",
        description: "Run a slow shell command in the background.",
        input_schema: {
          type: "object",
          properties: { command: { type: "string" } },
          required: ["command"]
        }
      },
      execute: (input2) => backgroundManager2.run(String(input2.command || ""))
    },
    {
      spec: {
        name: "check_background",
        description: "Check one background task or list all of them.",
        input_schema: {
          type: "object",
          properties: { task_id: { type: "string" } }
        }
      },
      execute: (input2) => backgroundManager2.check(input2.task_id ? String(input2.task_id) : void 0)
    }
  ];
}
var CronScheduler = class {
  scheduleFile;
  jobs = [];
  constructor(scheduleFile = path.join(WORKDIR, ".tasks", "cron.json")) {
    this.scheduleFile = scheduleFile;
    this.load();
  }
  load() {
    if (!fs.existsSync(this.scheduleFile)) {
      this.jobs = [];
      return;
    }
    this.jobs = JSON.parse(fs.readFileSync(this.scheduleFile, "utf8"));
  }
  save() {
    fs.mkdirSync(path.dirname(this.scheduleFile), { recursive: true });
    fs.writeFileSync(this.scheduleFile, JSON.stringify(this.jobs, null, 2), "utf8");
  }
  schedule(name, everyMinutes, taskTitle) {
    const job = { id: cryptoRandomId(), name, everyMinutes, taskTitle, lastRunAt: null };
    this.jobs.push(job);
    this.save();
    return `Scheduled ${name} every ${everyMinutes} minutes`;
  }
  list() {
    if (this.jobs.length === 0) {
      return "No scheduled jobs.";
    }
    return this.jobs.map((job) => `${job.id}: ${job.name} every ${job.everyMinutes}m -> ${job.taskTitle}`).join("\n");
  }
  runDue(taskManager2) {
    const now = Date.now();
    const dueJobs = this.jobs.filter((job) => job.lastRunAt === null || now - job.lastRunAt >= job.everyMinutes * 6e4);
    if (dueJobs.length === 0) {
      return "No jobs are due.";
    }
    for (const job of dueJobs) {
      taskManager2.createTask(job.taskTitle, `Scheduled by ${job.name}`);
      job.lastRunAt = now;
    }
    this.save();
    return `Ran ${dueJobs.length} scheduled job(s).`;
  }
};
function createCronTools(cronScheduler2, taskManager2) {
  return [
    {
      spec: {
        name: "schedule_task",
        description: "Schedule a recurring task-board item.",
        input_schema: {
          type: "object",
          properties: {
            name: { type: "string" },
            every_minutes: { type: "integer" },
            task_title: { type: "string" }
          },
          required: ["name", "every_minutes", "task_title"]
        }
      },
      execute: (input2) => cronScheduler2.schedule(
        String(input2.name || ""),
        Number(input2.every_minutes || 60),
        String(input2.task_title || "")
      )
    },
    {
      spec: {
        name: "list_schedules",
        description: "List recurring schedules.",
        input_schema: { type: "object", properties: {} }
      },
      execute: () => cronScheduler2.list()
    },
    {
      spec: {
        name: "run_due_schedules",
        description: "Materialize all due scheduled tasks into the task board.",
        input_schema: { type: "object", properties: {} }
      },
      execute: () => cronScheduler2.runDue(taskManager2)
    }
  ];
}
var MessageBus = class {
  inboxDir;
  constructor(inboxDir = path.join(WORKDIR, ".team", "inbox")) {
    this.inboxDir = inboxDir;
    fs.mkdirSync(this.inboxDir, { recursive: true });
  }
  send(recipient, payload) {
    const id = cryptoRandomId();
    const filePath = path.join(this.inboxDir, `${recipient}-${id}.json`);
    fs.writeFileSync(filePath, JSON.stringify({ id, recipient, ...payload }, null, 2), "utf8");
    return `Sent message ${id} to ${recipient}`;
  }
  read(recipient) {
    const files = fs.readdirSync(this.inboxDir).filter((fileName) => fileName.endsWith(".json"));
    const relevant = recipient ? files.filter((fileName) => fileName.startsWith(`${recipient}-`)) : files;
    if (relevant.length === 0) {
      return "Inbox is empty.";
    }
    return relevant.map((fileName) => {
      const payload = JSON.parse(fs.readFileSync(path.join(this.inboxDir, fileName), "utf8"));
      return `${payload.id}: ${payload.type || "message"} -> ${payload.subject || "(no subject)"}`;
    }).join("\n");
  }
};
var TeammateManager = class {
  teammateFile;
  teammates = [];
  constructor(teammateFile = path.join(WORKDIR, ".team", "teammates.json")) {
    this.teammateFile = teammateFile;
    this.load();
  }
  load() {
    if (!fs.existsSync(this.teammateFile)) {
      this.teammates = [];
      return;
    }
    this.teammates = JSON.parse(fs.readFileSync(this.teammateFile, "utf8"));
  }
  save() {
    fs.mkdirSync(path.dirname(this.teammateFile), { recursive: true });
    fs.writeFileSync(this.teammateFile, JSON.stringify(this.teammates, null, 2), "utf8");
  }
  register(name, role) {
    this.teammates = this.teammates.filter((teammate) => teammate.name !== name);
    this.teammates.push({ name, role });
    this.save();
    return `Registered teammate ${name} (${role})`;
  }
  list() {
    if (this.teammates.length === 0) {
      return "No teammates registered.";
    }
    return this.teammates.map((teammate) => `- ${teammate.name}: ${teammate.role}`).join("\n");
  }
};
function createTeamTools(messageBus2, teammateManager2) {
  return [
    {
      spec: {
        name: "register_teammate",
        description: "Register or update a teammate profile.",
        input_schema: {
          type: "object",
          properties: {
            name: { type: "string" },
            role: { type: "string" }
          },
          required: ["name", "role"]
        }
      },
      execute: (input2) => teammateManager2.register(String(input2.name || ""), String(input2.role || ""))
    },
    {
      spec: {
        name: "list_teammates",
        description: "List registered teammates.",
        input_schema: { type: "object", properties: {} }
      },
      execute: () => teammateManager2.list()
    },
    {
      spec: {
        name: "send_team_message",
        description: "Send a message into the shared team inbox.",
        input_schema: {
          type: "object",
          properties: {
            recipient: { type: "string" },
            type: { type: "string" },
            subject: { type: "string" },
            body: { type: "string" }
          },
          required: ["recipient", "type", "subject", "body"]
        }
      },
      execute: (input2) => messageBus2.send(String(input2.recipient || ""), {
        type: String(input2.type || "message"),
        subject: String(input2.subject || ""),
        body: String(input2.body || "")
      })
    },
    {
      spec: {
        name: "read_team_inbox",
        description: "Read inbox messages for one teammate or for everyone.",
        input_schema: {
          type: "object",
          properties: { recipient: { type: "string" } }
        }
      },
      execute: (input2) => messageBus2.read(input2.recipient ? String(input2.recipient) : void 0)
    }
  ];
}
function createProtocolTools(messageBus2) {
  return [
    {
      spec: {
        name: "request_plan_approval",
        description: "Send a structured plan approval request to another teammate.",
        input_schema: {
          type: "object",
          properties: {
            recipient: { type: "string" },
            subject: { type: "string" },
            plan: { type: "string" }
          },
          required: ["recipient", "subject", "plan"]
        }
      },
      execute: (input2) => messageBus2.send(String(input2.recipient || ""), {
        type: "plan_approval_request",
        subject: String(input2.subject || ""),
        body: String(input2.plan || "")
      })
    },
    {
      spec: {
        name: "respond_to_request",
        description: "Respond to a structured request in the team inbox.",
        input_schema: {
          type: "object",
          properties: {
            recipient: { type: "string" },
            approved: { type: "boolean" },
            response: { type: "string" }
          },
          required: ["recipient", "approved", "response"]
        }
      },
      execute: (input2) => messageBus2.send(String(input2.recipient || ""), {
        type: "plan_approval_response",
        approved: Boolean(input2.approved),
        body: String(input2.response || "")
      })
    }
  ];
}
var WorktreeManager = class {
  rootDir;
  constructor(rootDir = path.join(WORKDIR, ".worktrees")) {
    this.rootDir = rootDir;
    fs.mkdirSync(this.rootDir, { recursive: true });
  }
  createLane(name) {
    const lanePath = path.join(this.rootDir, name);
    fs.mkdirSync(lanePath, { recursive: true });
    return `Created isolated lane at ${path.relative(WORKDIR, lanePath)}`;
  }
  listLanes() {
    const lanes = fs.readdirSync(this.rootDir, { withFileTypes: true }).filter((entry) => entry.isDirectory()).map((entry) => entry.name);
    return lanes.length > 0 ? lanes.map((lane) => `- ${lane}`).join("\n") : "No isolated lanes.";
  }
  runInLane(name, command) {
    const lanePath = path.join(this.rootDir, name);
    if (!fs.existsSync(lanePath)) {
      return `Error: Unknown lane ${name}`;
    }
    return runBash(command, lanePath);
  }
};
function createWorktreeTools(worktreeManager2) {
  return [
    {
      spec: {
        name: "create_lane",
        description: "Create an isolated execution lane for a task.",
        input_schema: {
          type: "object",
          properties: { name: { type: "string" } },
          required: ["name"]
        }
      },
      execute: (input2) => worktreeManager2.createLane(String(input2.name || "lane"))
    },
    {
      spec: {
        name: "list_lanes",
        description: "List isolated execution lanes.",
        input_schema: { type: "object", properties: {} }
      },
      execute: () => worktreeManager2.listLanes()
    },
    {
      spec: {
        name: "run_in_lane",
        description: "Run a shell command inside an isolated lane.",
        input_schema: {
          type: "object",
          properties: {
            name: { type: "string" },
            command: { type: "string" }
          },
          required: ["name", "command"]
        }
      },
      execute: (input2) => worktreeManager2.runInLane(String(input2.name || ""), String(input2.command || ""))
    }
  ];
}
var PluginLoader = class {
  pluginDir;
  manifests = /* @__PURE__ */ new Map();
  constructor(pluginDir = path.join(WORKDIR, ".plugins")) {
    this.pluginDir = pluginDir;
    this.load();
  }
  load() {
    this.manifests.clear();
    if (!fs.existsSync(this.pluginDir)) {
      return;
    }
    for (const fileName of fs.readdirSync(this.pluginDir)) {
      if (!fileName.endsWith(".json")) {
        continue;
      }
      const parsed = JSON.parse(fs.readFileSync(path.join(this.pluginDir, fileName), "utf8"));
      this.manifests.set(parsed.name, parsed);
    }
  }
  list() {
    if (this.manifests.size === 0) {
      return "No plugins configured.";
    }
    return [...this.manifests.values()].map((manifest) => `- ${manifest.name}: ${manifest.description || manifest.command}`).join("\n");
  }
  describe(name) {
    const manifest = this.manifests.get(name);
    if (!manifest) {
      return `Error: Unknown plugin ${name}`;
    }
    return JSON.stringify(manifest, null, 2);
  }
};
function createPluginTools(pluginLoader2) {
  return [
    {
      spec: {
        name: "list_plugins",
        description: "List available plugin manifests.",
        input_schema: { type: "object", properties: {} }
      },
      execute: () => pluginLoader2.list()
    },
    {
      spec: {
        name: "describe_plugin",
        description: "Read one plugin manifest.",
        input_schema: {
          type: "object",
          properties: { name: { type: "string" } },
          required: ["name"]
        }
      },
      execute: (input2) => pluginLoader2.describe(String(input2.name || ""))
    }
  ];
}
function createTaskTool(runSubagent2) {
  return {
    spec: {
      name: "task",
      description: "Spawn a subagent with fresh context that returns only a summary.",
      input_schema: {
        type: "object",
        properties: {
          prompt: { type: "string" },
          description: { type: "string" }
        },
        required: ["prompt"]
      }
    },
    execute: async (input2) => runSubagent2(String(input2.prompt || ""))
  };
}
function createSubagentRunner(options) {
  return async (prompt) => {
    const subContext = createLoopContext();
    subContext.messages.push({ role: "user", content: prompt });
    await runAgentLoop(subContext, {
      systemPrompt: options.systemPrompt,
      tools: options.tools,
      normalizeMessages: options.normalizeMessages ?? true,
      maxTurns: options.maxTurns ?? 20
    });
    const lastAssistant = [...subContext.messages].reverse().find((message) => message.role === "assistant");
    return lastAssistant ? extractText(lastAssistant.content) || "(no summary)" : "(no summary)";
  };
}

// agents_self_contained/s_full.ts
var ctx = createLoopContext();
var compactManager = new CompactManager();
var permissionManager = new PermissionManager(process.env.PERMISSION_MODE || "default");
var hookManager = new HookManager();
var todoManager = new TodoManager();
var skillRegistry = new SkillRegistry();
var memoryManager = new MemoryManager();
var taskManager = new TaskManager();
var backgroundManager = new BackgroundManager();
var cronScheduler = new CronScheduler();
var messageBus = new MessageBus();
var teammateManager = new TeammateManager();
var worktreeManager = new WorktreeManager();
var pluginLoader = new PluginLoader();
memoryManager.loadAll();
var subagentTools = createBaseTools({
  compact: compactManager,
  permissions: permissionManager,
  hooks: hookManager
});
var runSubagent = createSubagentRunner({
  systemPrompt: `You are a coding subagent at ${WORKDIR}. Complete the delegated task, then return a short concrete summary.`,
  tools: subagentTools,
  normalizeMessages: true
});
var tools = [
  ...createBaseTools({
    compact: compactManager,
    permissions: permissionManager,
    hooks: hookManager
  }),
  createTodoTool(todoManager),
  createTaskTool(runSubagent),
  createLoadSkillTool(skillRegistry),
  createSaveMemoryTool(memoryManager),
  ...createTaskBoardTools(taskManager),
  ...createBackgroundTools(backgroundManager),
  ...createCronTools(cronScheduler, taskManager),
  ...createTeamTools(messageBus, teammateManager),
  ...createProtocolTools(messageBus),
  ...createWorktreeTools(worktreeManager),
  ...createPluginTools(pluginLoader)
];
await runRepl({
  label: "full",
  ctx,
  onSessionStart: () => {
    const sessionStart = hookManager.runHooks("SessionStart");
    if (sessionStart.messages.length > 0) {
      console.log(sessionStart.messages.join("\n"));
    }
  },
  run: async (loopCtx) => {
    memoryManager.loadAll();
    pluginLoader.load();
    await runAgentLoop(loopCtx, {
      systemPrompt: `You are a capstone teaching agent at ${WORKDIR}.
Use the available tools to plan, delegate, remember durable facts, coordinate background work, and keep context under control.

Skills:
${skillRegistry.describeAvailable()}

Memories:
${memoryManager.loadMemoryPrompt() || "(no saved memory yet)"}`.trim(),
      tools,
      normalizeMessages: true,
      beforeModel: async () => {
        const notifications = backgroundManager.drainNotifications();
        if (notifications.length > 0) {
          loopCtx.messages.push({
            role: "user",
            content: notifications.map((item) => `Background task ${item.taskId} finished [${item.status}] -> ${item.preview} (output_file=${item.outputFile})`).join("\n")
          });
        }
        if (compactManager.estimateContextSize(loopCtx.messages) > compactManager.contextLimit) {
          loopCtx.messages = await compactManager.compactHistory(loopCtx.messages, "Continue the active coding task.");
        }
      },
      afterToolResults: () => {
        compactManager.microCompact(loopCtx.messages);
      },
      onModelError: (error, currentCtx, attempt) => {
        if (attempt > 1) {
          return false;
        }
        currentCtx.messages.push({
          role: "user",
          content: `The previous model call failed with: ${error instanceof Error ? error.message : String(error)}. Recover and continue with the current task state.`
        });
        return true;
      }
    });
  }
});
