{
  "versions": [
    {
      "id": "s01",
      "filename": "s01_agent_loop.ts",
      "title": "The Agent Loop",
      "subtitle": "Minimal Closed Loop",
      "loc": 393,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "newTools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "coreAddition": "LoopState + tool_result feedback",
      "keyInsight": "An agent is just a loop: send messages, execute tools, feed results back, repeat.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        }
      ],
      "layer": "core",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s01_agent_loop.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\n\n// agents_self_contained/s01_agent_loop.ts\nvar ctx = createLoopContext();\nvar tools = [createBaseTools()[0]];\nawait runRepl({\n  label: \"s01\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use bash to inspect and change the workspace. Act first, then report clearly.`,\n      tools,\n      maxTurns: 20\n    });\n  }\n});\n"
    },
    {
      "id": "s02",
      "filename": "s02_tool_use.ts",
      "title": "Tool Use",
      "subtitle": "Route Intent into Action",
      "loc": 393,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "newTools": [],
      "coreAddition": "Tool specs + dispatch map",
      "keyInsight": "Adding a tool means adding one handler. The loop never changes.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        }
      ],
      "layer": "core",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s02_tool_use.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\n\n// agents_self_contained/s02_tool_use.ts\nvar ctx = createLoopContext();\nvar tools = createBaseTools();\nawait runRepl({\n  label: \"s02\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use tools to solve tasks. Act, don't explain.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s03",
      "filename": "s03_todo_write.ts",
      "title": "TodoWrite",
      "subtitle": "Session Planning",
      "loc": 484,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "todo"
      ],
      "newTools": [
        "todo"
      ],
      "coreAddition": "PlanningState + reminder loop",
      "keyInsight": "A visible plan keeps the agent on track when tasks get complex.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTodoTool",
          "signature": "function createTodoTool(todoManager2) {",
          "startLine": 437
        }
      ],
      "layer": "core",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s03_todo_write.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar TodoManager = class {\n  items = [];\n  roundsSinceUpdate = 0;\n  update(items) {\n    if (items.length > 12) {\n      throw new Error(\"Keep the session plan short (max 12 items)\");\n    }\n    const normalized = items.map((item, index) => {\n      const content = String(item.content || \"\").trim();\n      const status = String(item.status || \"pending\");\n      const activeForm = String(item.activeForm || \"\");\n      if (!content) {\n        throw new Error(`Item ${index}: content required`);\n      }\n      if (![\"pending\", \"in_progress\", \"completed\"].includes(status)) {\n        throw new Error(`Item ${index}: invalid status '${status}'`);\n      }\n      return { content, status, activeForm };\n    });\n    const active = normalized.filter((item) => item.status === \"in_progress\");\n    if (active.length > 1) {\n      throw new Error(\"Only one plan item can be in_progress\");\n    }\n    this.items = normalized;\n    this.roundsSinceUpdate = 0;\n    return this.render();\n  }\n  noteRoundWithoutUpdate() {\n    this.roundsSinceUpdate += 1;\n  }\n  reminder(interval = 3) {\n    if (this.items.length === 0 || this.roundsSinceUpdate < interval) {\n      return null;\n    }\n    return \"<reminder>Refresh your current plan before continuing.</reminder>\";\n  }\n  render() {\n    if (this.items.length === 0) {\n      return \"No session plan yet.\";\n    }\n    const lines = this.items.map((item) => {\n      const marker = item.status === \"completed\" ? \"[x]\" : item.status === \"in_progress\" ? \"[>]\" : \"[ ]\";\n      return item.status === \"in_progress\" && item.activeForm ? `${marker} ${item.content} (${item.activeForm})` : `${marker} ${item.content}`;\n    });\n    const completed = this.items.filter((item) => item.status === \"completed\").length;\n    lines.push(`\n(${completed}/${this.items.length} completed)`);\n    return lines.join(\"\\n\");\n  }\n};\nfunction createTodoTool(todoManager2) {\n  return {\n    spec: {\n      name: \"todo\",\n      description: \"Rewrite the current session plan for multi-step work.\",\n      input_schema: {\n        type: \"object\",\n        properties: {\n          items: {\n            type: \"array\",\n            items: {\n              type: \"object\",\n              properties: {\n                content: { type: \"string\" },\n                status: { type: \"string\", enum: [\"pending\", \"in_progress\", \"completed\"] },\n                activeForm: { type: \"string\" }\n              },\n              required: [\"content\", \"status\"]\n            }\n          }\n        },\n        required: [\"items\"]\n      }\n    },\n    execute: (input2) => {\n      const items = Array.isArray(input2.items) ? input2.items : [];\n      return todoManager2.update(items);\n    }\n  };\n}\n\n// agents_self_contained/s03_todo_write.ts\nvar ctx = createLoopContext();\nvar todoManager = new TodoManager();\nvar tools = [...createBaseTools(), createTodoTool(todoManager)];\nawait runRepl({\n  label: \"s03\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}.\nUse the todo tool for multi-step work.\nKeep exactly one step in_progress when a task has multiple steps.\nRefresh the plan as work advances. Prefer tools over prose.`,\n      tools,\n      normalizeMessages: true,\n      afterToolResults: () => {\n        todoManager.noteRoundWithoutUpdate();\n        const reminder = todoManager.reminder();\n        if (reminder) {\n          loopCtx.messages.push({ role: \"user\", content: reminder });\n        }\n      }\n    });\n  }\n});\n"
    },
    {
      "id": "s04",
      "filename": "s04_subagent.ts",
      "title": "Subagent",
      "subtitle": "Fresh Context per Subtask",
      "loc": 430,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "task"
      ],
      "newTools": [
        "task"
      ],
      "coreAddition": "Delegation with isolated message history",
      "keyInsight": "A subagent is mainly a context boundary, not a process trick.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTaskTool",
          "signature": "function createTaskTool(runSubagent2) {",
          "startLine": 387
        },
        {
          "name": "createSubagentRunner",
          "signature": "function createSubagentRunner(options) {",
          "startLine": 404
        }
      ],
      "layer": "core",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s04_subagent.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nfunction createTaskTool(runSubagent2) {\n  return {\n    spec: {\n      name: \"task\",\n      description: \"Spawn a subagent with fresh context that returns only a summary.\",\n      input_schema: {\n        type: \"object\",\n        properties: {\n          prompt: { type: \"string\" },\n          description: { type: \"string\" }\n        },\n        required: [\"prompt\"]\n      }\n    },\n    execute: async (input2) => runSubagent2(String(input2.prompt || \"\"))\n  };\n}\nfunction createSubagentRunner(options) {\n  return async (prompt) => {\n    const subContext = createLoopContext();\n    subContext.messages.push({ role: \"user\", content: prompt });\n    await runAgentLoop(subContext, {\n      systemPrompt: options.systemPrompt,\n      tools: options.tools,\n      normalizeMessages: options.normalizeMessages ?? true,\n      maxTurns: options.maxTurns ?? 20\n    });\n    const lastAssistant = [...subContext.messages].reverse().find((message) => message.role === \"assistant\");\n    return lastAssistant ? extractText(lastAssistant.content) || \"(no summary)\" : \"(no summary)\";\n  };\n}\n\n// agents_self_contained/s04_subagent.ts\nvar ctx = createLoopContext();\nvar childTools = createBaseTools();\nvar runSubagent = createSubagentRunner({\n  systemPrompt: `You are a coding subagent at ${WORKDIR}. Complete the given task, then summarize your findings.`,\n  tools: childTools,\n  normalizeMessages: true\n});\nvar tools = [...createBaseTools(), createTaskTool(runSubagent)];\nawait runRepl({\n  label: \"s04\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use the task tool to delegate exploration or subtasks.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s05",
      "filename": "s05_skill_loading.ts",
      "title": "Skills",
      "subtitle": "Discover Cheap, Load Deep",
      "loc": 481,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "load_skill"
      ],
      "newTools": [
        "load_skill"
      ],
      "coreAddition": "Skill registry + on-demand injection",
      "keyInsight": "Discover cheaply, load deeply -- only when needed.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "parseFrontmatter",
          "signature": "function parseFrontmatter(text) {",
          "startLine": 387
        },
        {
          "name": "walkFiles",
          "signature": "function walkFiles(root) {",
          "startLine": 445
        },
        {
          "name": "createLoadSkillTool",
          "signature": "function createLoadSkillTool(skillRegistry2) {",
          "startLine": 457
        }
      ],
      "layer": "core",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s05_skill_loading.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nfunction parseFrontmatter(text) {\n  const match = /^---\\n([\\s\\S]*?)\\n---\\n?([\\s\\S]*)$/u.exec(text);\n  if (!match) {\n    return { meta: {}, body: text };\n  }\n  const meta = {};\n  for (const line of match[1].split(\"\\n\")) {\n    const separator = line.indexOf(\":\");\n    if (separator === -1) {\n      continue;\n    }\n    meta[line.slice(0, separator).trim()] = line.slice(separator + 1).trim();\n  }\n  return { meta, body: match[2].trim() };\n}\nvar SkillRegistry = class {\n  skillsDir;\n  documents = /* @__PURE__ */ new Map();\n  constructor(skillsDir = path.join(WORKDIR, \"skills\")) {\n    this.skillsDir = skillsDir;\n    this.loadAll();\n  }\n  loadAll() {\n    this.documents.clear();\n    if (!fs.existsSync(this.skillsDir)) {\n      return;\n    }\n    const entries = walkFiles(this.skillsDir).filter((filePath) => filePath.endsWith(\"SKILL.md\"));\n    for (const filePath of entries) {\n      const parsed = parseFrontmatter(fs.readFileSync(filePath, \"utf8\"));\n      const name = parsed.meta.name || path.basename(path.dirname(filePath));\n      const description = parsed.meta.description || \"No description\";\n      this.documents.set(name, {\n        name,\n        description,\n        path: filePath,\n        body: parsed.body\n      });\n    }\n  }\n  describeAvailable() {\n    if (this.documents.size === 0) {\n      return \"(no skills available)\";\n    }\n    return [...this.documents.values()].sort((a, b) => a.name.localeCompare(b.name)).map((skill) => `- ${skill.name}: ${skill.description}`).join(\"\\n\");\n  }\n  loadFullText(name) {\n    const skill = this.documents.get(name);\n    if (!skill) {\n      const known = [...this.documents.keys()].sort().join(\", \") || \"(none)\";\n      return `Error: Unknown skill '${name}'. Available skills: ${known}`;\n    }\n    const relPath = path.relative(WORKDIR, skill.path);\n    return `<skill name=\"${skill.name}\" path=\"${relPath}\">\n${skill.body}\n</skill>`;\n  }\n};\nfunction walkFiles(root) {\n  const results = [];\n  for (const entry of fs.readdirSync(root, { withFileTypes: true })) {\n    const fullPath = path.join(root, entry.name);\n    if (entry.isDirectory()) {\n      results.push(...walkFiles(fullPath));\n      continue;\n    }\n    results.push(fullPath);\n  }\n  return results;\n}\nfunction createLoadSkillTool(skillRegistry2) {\n  return {\n    spec: {\n      name: \"load_skill\",\n      description: \"Load the full body of a named skill into the current context.\",\n      input_schema: {\n        type: \"object\",\n        properties: { name: { type: \"string\" } },\n        required: [\"name\"]\n      }\n    },\n    execute: (input2) => skillRegistry2.loadFullText(String(input2.name || \"\"))\n  };\n}\n\n// agents_self_contained/s05_skill_loading.ts\nvar ctx = createLoopContext();\nvar skillRegistry = new SkillRegistry();\nvar tools = [...createBaseTools(), createLoadSkillTool(skillRegistry)];\nawait runRepl({\n  label: \"s05\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}.\nUse load_skill when a task needs specialized instructions before you act.\n\nSkills available:\n${skillRegistry.describeAvailable()}`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s06",
      "filename": "s06_context_compact.ts",
      "title": "Context Compact",
      "subtitle": "Keep the Active Context Small",
      "loc": 493,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "newTools": [],
      "coreAddition": "Persist markers + micro compact + summary compact",
      "keyInsight": "Compaction isn't deleting history -- it's relocating detail so the agent can keep working.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 19
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 22
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 32
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 35
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 43
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 70
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 83
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 93
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 108
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 122
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 133
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 175
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 185
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 237
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 261
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 264
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 298
        }
      ],
      "layer": "core",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s06_context_compact.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport fsp from \"node:fs/promises\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar CompactManager = class {\n  contextLimit;\n  persistThreshold;\n  previewChars;\n  keepRecentToolResults;\n  transcriptDir;\n  toolResultsDir;\n  recentFiles = [];\n  constructor(options) {\n    this.contextLimit = options?.contextLimit ?? 5e4;\n    this.persistThreshold = options?.persistThreshold ?? 3e4;\n    this.previewChars = options?.previewChars ?? 2e3;\n    this.keepRecentToolResults = options?.keepRecentToolResults ?? 3;\n    this.transcriptDir = path.join(WORKDIR, \".transcripts\");\n    this.toolResultsDir = path.join(WORKDIR, \".task_outputs\", \"tool-results\");\n  }\n  estimateContextSize(messages) {\n    return JSON.stringify(messages).length;\n  }\n  trackRecentFile(filePath) {\n    this.recentFiles = this.recentFiles.filter((item) => item !== filePath);\n    this.recentFiles.push(filePath);\n    this.recentFiles = this.recentFiles.slice(-5);\n  }\n  persistLargeOutput(toolUseId, output2) {\n    if (output2.length <= this.persistThreshold) {\n      return output2;\n    }\n    fs.mkdirSync(this.toolResultsDir, { recursive: true });\n    const safeId = toolUseId.replace(/[^a-zA-Z0-9_.-]/gu, \"_\");\n    const filePath = path.join(this.toolResultsDir, `${safeId}.txt`);\n    if (!fs.existsSync(filePath)) {\n      fs.writeFileSync(filePath, output2, \"utf8\");\n    }\n    const preview = output2.slice(0, this.previewChars);\n    return [\n      \"<persisted-output>\",\n      `Full output saved to: ${path.relative(WORKDIR, filePath)}`,\n      \"Preview:\",\n      preview,\n      \"</persisted-output>\"\n    ].join(\"\\n\");\n  }\n  microCompact(messages) {\n    const toolBlocks = [];\n    for (const message of messages) {\n      if (message.role !== \"user\" || !Array.isArray(message.content)) {\n        continue;\n      }\n      for (const block of message.content) {\n        if (typeof block === \"object\" && block !== null && block.type === \"tool_result\") {\n          toolBlocks.push(block);\n        }\n      }\n    }\n    for (const block of toolBlocks.slice(0, -this.keepRecentToolResults)) {\n      if (block.content.length > 120) {\n        block.content = \"[Earlier tool result compacted. Re-run the tool if you need full detail.]\";\n      }\n    }\n  }\n  async writeTranscript(messages) {\n    await fsp.mkdir(this.transcriptDir, { recursive: true });\n    const filePath = path.join(this.transcriptDir, `transcript_${Date.now()}.jsonl`);\n    const lines = messages.map((message) => JSON.stringify(message)).join(\"\\n\");\n    await fsp.writeFile(filePath, `${lines}\n`, \"utf8\");\n    return filePath;\n  }\n  async compactHistory(messages, focus) {\n    await this.writeTranscript(messages);\n    const lastUserGoal = [...messages].reverse().find((message) => message.role === \"user\");\n    const summaryLines = [\n      \"This conversation was compacted so the agent can continue working.\",\n      \"\",\n      \"Current goal:\",\n      extractText(lastUserGoal?.content || \"\"),\n      \"\",\n      \"What happened recently:\",\n      ...messages.slice(-6).map((message) => `- ${message.role}: ${extractText(message.content).slice(0, 160)}`)\n    ];\n    if (this.recentFiles.length > 0) {\n      summaryLines.push(\"\", \"Recent files to reopen:\", ...this.recentFiles.map((item) => `- ${item}`));\n    }\n    if (focus) {\n      summaryLines.push(\"\", `Focus to preserve: ${focus}`);\n    }\n    return [{ role: \"user\", content: summaryLines.join(\"\\n\") }];\n  }\n};\n\n// agents_self_contained/s06_context_compact.ts\nvar ctx = createLoopContext();\nvar compactManager = new CompactManager();\nvar tools = createBaseTools({ compact: compactManager });\nawait runRepl({\n  label: \"s06\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Keep working step by step, and compact context when it gets too long.`,\n      tools,\n      normalizeMessages: true,\n      beforeModel: async () => {\n        if (compactManager.estimateContextSize(loopCtx.messages) > compactManager.contextLimit) {\n          loopCtx.messages = await compactManager.compactHistory(loopCtx.messages, \"Continue the current implementation task.\");\n        }\n      },\n      afterToolResults: () => {\n        compactManager.microCompact(loopCtx.messages);\n      }\n    });\n  }\n});\n"
    },
    {
      "id": "s07",
      "filename": "s07_permission_system.ts",
      "title": "Permission System",
      "subtitle": "Intent Must Pass Safety",
      "loc": 458,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "newTools": [],
      "coreAddition": "deny / mode / allow / ask pipeline",
      "keyInsight": "Safety is a pipeline, not a boolean: deny, check mode, allow, then ask.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        }
      ],
      "layer": "hardening",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s07_permission_system.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process2 from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process2.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process2.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process2.env.ANTHROPIC_API_KEY || process2.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process2.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar PermissionManager = class {\n  mode;\n  rules = [\n    { tool: \"bash\", content: \"rm -rf /\", behavior: \"deny\" },\n    { tool: \"bash\", content: \"sudo \", behavior: \"deny\" },\n    { tool: \"read_file\", path: \"*\", behavior: \"allow\" }\n  ];\n  consecutiveDenials = 0;\n  constructor(mode = \"default\") {\n    this.mode = mode;\n  }\n  check(toolName, toolInput) {\n    if (toolName === \"bash\") {\n      const command = String(toolInput.command || \"\");\n      if (/(^|\\s)sudo(\\s|$)/u.test(command) || /\\brm\\s+-[a-zA-Z]*r/u.test(command)) {\n        return { behavior: \"deny\", reason: \"Bash validator flagged a high-risk command\" };\n      }\n      if (/[;&|`]/u.test(command) || /\\$\\(/u.test(command)) {\n        return { behavior: \"ask\", reason: \"Bash validator flagged shell metacharacters\" };\n      }\n    }\n    for (const rule of this.rules.filter((rule2) => rule2.behavior === \"deny\")) {\n      if (this.matches(rule, toolName, toolInput)) {\n        return { behavior: \"deny\", reason: `Blocked by deny rule for ${toolName}` };\n      }\n    }\n    if (this.mode === \"plan\") {\n      return [\"read_file\"].includes(toolName) ? { behavior: \"allow\", reason: \"Plan mode allows read-only tools\" } : { behavior: \"deny\", reason: \"Plan mode blocks state changes\" };\n    }\n    if (this.mode === \"auto\" && [\"read_file\"].includes(toolName)) {\n      return { behavior: \"allow\", reason: \"Auto mode allows read-only tools\" };\n    }\n    for (const rule of this.rules.filter((rule2) => rule2.behavior === \"allow\")) {\n      if (this.matches(rule, toolName, toolInput)) {\n        return { behavior: \"allow\", reason: `Matched allow rule for ${toolName}` };\n      }\n    }\n    return { behavior: \"ask\", reason: `No rule matched for ${toolName}` };\n  }\n  askUser(toolName, toolInput) {\n    const preview = JSON.stringify(toolInput).slice(0, 200);\n    const result = spawnSync(\"/bin/zsh\", [\"-lc\", 'printf \"\\\\n[Permission] %s: %s\\\\nAllow? (y/n): \" \"$TOOL_NAME\" \"$TOOL_PREVIEW\"; read answer; [[ \"$answer\" == \"y\" || \"$answer\" == \"yes\" ]]'], {\n      env: {\n        ...process2.env,\n        TOOL_NAME: toolName,\n        TOOL_PREVIEW: preview\n      },\n      stdio: \"inherit\"\n    });\n    return result.status === 0;\n  }\n  matches(rule, toolName, toolInput) {\n    if (rule.tool !== \"*\" && rule.tool !== toolName) {\n      return false;\n    }\n    if (rule.path && rule.path !== \"*\") {\n      return String(toolInput.path || \"\").includes(rule.path.replace(\"*\", \"\"));\n    }\n    if (rule.content) {\n      return String(toolInput.command || \"\").includes(rule.content.replace(\"*\", \"\"));\n    }\n    return true;\n  }\n};\n\n// agents_self_contained/s07_permission_system.ts\nvar ctx = createLoopContext();\nvar permissions = new PermissionManager(process.env.PERMISSION_MODE || \"default\");\nvar tools = createBaseTools({ permissions });\nawait runRepl({\n  label: \"s07\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Every tool call passes through a permission pipeline before execution.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s08",
      "filename": "s08_hook_system.ts",
      "title": "Hook System",
      "subtitle": "Extend Without Rewriting the Loop",
      "loc": 455,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "newTools": [],
      "coreAddition": "Lifecycle events + side-effect hooks",
      "keyInsight": "The loop owns control flow; hooks only observe, block, or annotate at named moments.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        }
      ],
      "layer": "hardening",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s08_hook_system.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar HookManager = class {\n  hooks = {\n    PreToolUse: [],\n    PostToolUse: [],\n    SessionStart: []\n  };\n  constructor(configPath = path.join(WORKDIR, \".hooks.json\")) {\n    if (!fs.existsSync(configPath)) {\n      return;\n    }\n    try {\n      const parsed = JSON.parse(fs.readFileSync(configPath, \"utf8\"));\n      this.hooks = {\n        PreToolUse: parsed.hooks?.PreToolUse || [],\n        PostToolUse: parsed.hooks?.PostToolUse || [],\n        SessionStart: parsed.hooks?.SessionStart || []\n      };\n    } catch (error) {\n      console.error(`[Hook config error] ${error instanceof Error ? error.message : String(error)}`);\n    }\n  }\n  runHooks(event, context) {\n    const result = { blocked: false, blockReason: void 0, messages: [] };\n    const trustMarker = path.join(WORKDIR, \".claude\", \".claude_trusted\");\n    if (!fs.existsSync(trustMarker)) {\n      return result;\n    }\n    for (const hook of this.hooks[event]) {\n      if (hook.matcher && context?.tool_name && hook.matcher !== \"*\" && hook.matcher !== context.tool_name) {\n        continue;\n      }\n      const env = {\n        ...process.env,\n        HOOK_EVENT: event,\n        HOOK_TOOL_NAME: String(context?.tool_name || \"\"),\n        HOOK_TOOL_INPUT: JSON.stringify(context?.tool_input || {}),\n        HOOK_TOOL_OUTPUT: String(context?.tool_output || \"\")\n      };\n      const executed = spawnSync(\"/bin/zsh\", [\"-lc\", hook.command], {\n        cwd: WORKDIR,\n        env,\n        encoding: \"utf8\",\n        timeout: 3e4\n      });\n      if (executed.status === 1) {\n        result.blocked = true;\n        result.blockReason = executed.stderr?.trim() || \"Blocked by hook\";\n      }\n      if (executed.status === 2 && executed.stderr?.trim()) {\n        result.messages.push(executed.stderr.trim());\n      }\n    }\n    return result;\n  }\n};\n\n// agents_self_contained/s08_hook_system.ts\nvar ctx = createLoopContext();\nvar hooks = new HookManager();\nvar tools = createBaseTools({ hooks });\nawait runRepl({\n  label: \"s08\",\n  ctx,\n  onSessionStart: () => {\n    const result = hooks.runHooks(\"SessionStart\");\n    if (result.messages.length > 0) {\n      console.log(result.messages.join(\"\\n\"));\n    }\n  },\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Hooks extend the agent without touching the loop.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s09",
      "filename": "s09_memory_system.ts",
      "title": "Memory System",
      "subtitle": "Keep Only What Survives Sessions",
      "loc": 518,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "save_memory"
      ],
      "newTools": [
        "save_memory"
      ],
      "coreAddition": "Typed memory records + reload path",
      "keyInsight": "Memory gives direction; current observation gives truth.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "parseFrontmatter",
          "signature": "function parseFrontmatter(text) {",
          "startLine": 387
        },
        {
          "name": "createSaveMemoryTool",
          "signature": "function createSaveMemoryTool(memoryManager2) {",
          "startLine": 483
        }
      ],
      "layer": "hardening",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s09_memory_system.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nfunction parseFrontmatter(text) {\n  const match = /^---\\n([\\s\\S]*?)\\n---\\n?([\\s\\S]*)$/u.exec(text);\n  if (!match) {\n    return { meta: {}, body: text };\n  }\n  const meta = {};\n  for (const line of match[1].split(\"\\n\")) {\n    const separator = line.indexOf(\":\");\n    if (separator === -1) {\n      continue;\n    }\n    meta[line.slice(0, separator).trim()] = line.slice(separator + 1).trim();\n  }\n  return { meta, body: match[2].trim() };\n}\nvar MEMORY_TYPES = [\"user\", \"feedback\", \"project\", \"reference\"];\nvar MemoryManager = class {\n  memoryDir;\n  memories = /* @__PURE__ */ new Map();\n  constructor(memoryDir = path.join(WORKDIR, \".memory\")) {\n    this.memoryDir = memoryDir;\n  }\n  loadAll() {\n    this.memories.clear();\n    if (!fs.existsSync(this.memoryDir)) {\n      return;\n    }\n    for (const fileName of fs.readdirSync(this.memoryDir)) {\n      if (!fileName.endsWith(\".md\") || fileName === \"MEMORY.md\") {\n        continue;\n      }\n      const parsed = parseFrontmatter(fs.readFileSync(path.join(this.memoryDir, fileName), \"utf8\"));\n      const name = parsed.meta.name || fileName.replace(/\\.md$/u, \"\");\n      const type = parsed.meta.type || \"project\";\n      this.memories.set(name, {\n        description: parsed.meta.description || \"\",\n        type,\n        content: parsed.body,\n        file: fileName\n      });\n    }\n  }\n  loadMemoryPrompt() {\n    if (this.memories.size === 0) {\n      return \"\";\n    }\n    const lines = [\"# Memories (persistent across sessions)\", \"\"];\n    for (const memoryType of MEMORY_TYPES) {\n      const entries = [...this.memories.entries()].filter(([, value]) => value.type === memoryType);\n      if (entries.length === 0) {\n        continue;\n      }\n      lines.push(`## [${memoryType}]`);\n      for (const [name, value] of entries) {\n        lines.push(`### ${name}: ${value.description}`);\n        if (value.content.trim()) {\n          lines.push(value.content.trim());\n        }\n        lines.push(\"\");\n      }\n    }\n    return lines.join(\"\\n\");\n  }\n  saveMemory(name, description, memoryType, content) {\n    if (!MEMORY_TYPES.includes(memoryType)) {\n      return `Error: type must be one of ${MEMORY_TYPES.join(\", \")}`;\n    }\n    const safeName = name.toLowerCase().replace(/[^a-z0-9_-]/gu, \"_\") || \"memory\";\n    fs.mkdirSync(this.memoryDir, { recursive: true });\n    const fileName = `${safeName}.md`;\n    const filePath = path.join(this.memoryDir, fileName);\n    fs.writeFileSync(\n      filePath,\n      `---\nname: ${name}\ndescription: ${description}\ntype: ${memoryType}\n---\n${content}\n`,\n      \"utf8\"\n    );\n    this.memories.set(name, { description, type: memoryType, content, file: fileName });\n    this.rebuildIndex();\n    return `Saved memory '${name}' [${memoryType}] to ${path.relative(WORKDIR, filePath)}`;\n  }\n  rebuildIndex() {\n    fs.mkdirSync(this.memoryDir, { recursive: true });\n    const lines = [\"# Memory Index\", \"\"];\n    for (const [name, value] of this.memories.entries()) {\n      lines.push(`- ${name}: ${value.description} [${value.type}]`);\n    }\n    fs.writeFileSync(path.join(this.memoryDir, \"MEMORY.md\"), `${lines.join(\"\\n\")}\n`, \"utf8\");\n  }\n};\nfunction createSaveMemoryTool(memoryManager2) {\n  return {\n    spec: {\n      name: \"save_memory\",\n      description: \"Save durable cross-session information to the memory store.\",\n      input_schema: {\n        type: \"object\",\n        properties: {\n          name: { type: \"string\" },\n          description: { type: \"string\" },\n          type: { type: \"string\", enum: [...MEMORY_TYPES] },\n          content: { type: \"string\" }\n        },\n        required: [\"name\", \"description\", \"type\", \"content\"]\n      }\n    },\n    execute: (input2) => memoryManager2.saveMemory(\n      String(input2.name || \"\"),\n      String(input2.description || \"\"),\n      String(input2.type || \"project\"),\n      String(input2.content || \"\")\n    )\n  };\n}\n\n// agents_self_contained/s09_memory_system.ts\nvar ctx = createLoopContext();\nvar memoryManager = new MemoryManager();\nmemoryManager.loadAll();\nvar tools = [...createBaseTools(), createSaveMemoryTool(memoryManager)];\nawait runRepl({\n  label: \"s09\",\n  ctx,\n  run: async (loopCtx) => {\n    memoryManager.loadAll();\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}.\nUse memory only for durable information that should survive the current session.\n\n${memoryManager.loadMemoryPrompt()}`.trim(),\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s10",
      "filename": "s10_system_prompt.ts",
      "title": "System Prompt",
      "subtitle": "Build Inputs as a Pipeline",
      "loc": 595,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "load_skill",
        "save_memory"
      ],
      "newTools": [
        "load_skill"
      ],
      "coreAddition": "Prompt sections + dynamic assembly",
      "keyInsight": "The model sees a constructed input pipeline, not one giant static string.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "parseFrontmatter",
          "signature": "function parseFrontmatter(text) {",
          "startLine": 387
        },
        {
          "name": "walkFiles",
          "signature": "function walkFiles(root) {",
          "startLine": 445
        },
        {
          "name": "createLoadSkillTool",
          "signature": "function createLoadSkillTool(skillRegistry2) {",
          "startLine": 457
        },
        {
          "name": "createSaveMemoryTool",
          "signature": "function createSaveMemoryTool(memoryManager2) {",
          "startLine": 552
        },
        {
          "name": "buildPromptSections",
          "signature": "function buildPromptSections(sections) {",
          "startLine": 576
        }
      ],
      "layer": "hardening",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s10_system_prompt.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nfunction parseFrontmatter(text) {\n  const match = /^---\\n([\\s\\S]*?)\\n---\\n?([\\s\\S]*)$/u.exec(text);\n  if (!match) {\n    return { meta: {}, body: text };\n  }\n  const meta = {};\n  for (const line of match[1].split(\"\\n\")) {\n    const separator = line.indexOf(\":\");\n    if (separator === -1) {\n      continue;\n    }\n    meta[line.slice(0, separator).trim()] = line.slice(separator + 1).trim();\n  }\n  return { meta, body: match[2].trim() };\n}\nvar SkillRegistry = class {\n  skillsDir;\n  documents = /* @__PURE__ */ new Map();\n  constructor(skillsDir = path.join(WORKDIR, \"skills\")) {\n    this.skillsDir = skillsDir;\n    this.loadAll();\n  }\n  loadAll() {\n    this.documents.clear();\n    if (!fs.existsSync(this.skillsDir)) {\n      return;\n    }\n    const entries = walkFiles(this.skillsDir).filter((filePath) => filePath.endsWith(\"SKILL.md\"));\n    for (const filePath of entries) {\n      const parsed = parseFrontmatter(fs.readFileSync(filePath, \"utf8\"));\n      const name = parsed.meta.name || path.basename(path.dirname(filePath));\n      const description = parsed.meta.description || \"No description\";\n      this.documents.set(name, {\n        name,\n        description,\n        path: filePath,\n        body: parsed.body\n      });\n    }\n  }\n  describeAvailable() {\n    if (this.documents.size === 0) {\n      return \"(no skills available)\";\n    }\n    return [...this.documents.values()].sort((a, b) => a.name.localeCompare(b.name)).map((skill) => `- ${skill.name}: ${skill.description}`).join(\"\\n\");\n  }\n  loadFullText(name) {\n    const skill = this.documents.get(name);\n    if (!skill) {\n      const known = [...this.documents.keys()].sort().join(\", \") || \"(none)\";\n      return `Error: Unknown skill '${name}'. Available skills: ${known}`;\n    }\n    const relPath = path.relative(WORKDIR, skill.path);\n    return `<skill name=\"${skill.name}\" path=\"${relPath}\">\n${skill.body}\n</skill>`;\n  }\n};\nfunction walkFiles(root) {\n  const results = [];\n  for (const entry of fs.readdirSync(root, { withFileTypes: true })) {\n    const fullPath = path.join(root, entry.name);\n    if (entry.isDirectory()) {\n      results.push(...walkFiles(fullPath));\n      continue;\n    }\n    results.push(fullPath);\n  }\n  return results;\n}\nfunction createLoadSkillTool(skillRegistry2) {\n  return {\n    spec: {\n      name: \"load_skill\",\n      description: \"Load the full body of a named skill into the current context.\",\n      input_schema: {\n        type: \"object\",\n        properties: { name: { type: \"string\" } },\n        required: [\"name\"]\n      }\n    },\n    execute: (input2) => skillRegistry2.loadFullText(String(input2.name || \"\"))\n  };\n}\nvar MEMORY_TYPES = [\"user\", \"feedback\", \"project\", \"reference\"];\nvar MemoryManager = class {\n  memoryDir;\n  memories = /* @__PURE__ */ new Map();\n  constructor(memoryDir = path.join(WORKDIR, \".memory\")) {\n    this.memoryDir = memoryDir;\n  }\n  loadAll() {\n    this.memories.clear();\n    if (!fs.existsSync(this.memoryDir)) {\n      return;\n    }\n    for (const fileName of fs.readdirSync(this.memoryDir)) {\n      if (!fileName.endsWith(\".md\") || fileName === \"MEMORY.md\") {\n        continue;\n      }\n      const parsed = parseFrontmatter(fs.readFileSync(path.join(this.memoryDir, fileName), \"utf8\"));\n      const name = parsed.meta.name || fileName.replace(/\\.md$/u, \"\");\n      const type = parsed.meta.type || \"project\";\n      this.memories.set(name, {\n        description: parsed.meta.description || \"\",\n        type,\n        content: parsed.body,\n        file: fileName\n      });\n    }\n  }\n  loadMemoryPrompt() {\n    if (this.memories.size === 0) {\n      return \"\";\n    }\n    const lines = [\"# Memories (persistent across sessions)\", \"\"];\n    for (const memoryType of MEMORY_TYPES) {\n      const entries = [...this.memories.entries()].filter(([, value]) => value.type === memoryType);\n      if (entries.length === 0) {\n        continue;\n      }\n      lines.push(`## [${memoryType}]`);\n      for (const [name, value] of entries) {\n        lines.push(`### ${name}: ${value.description}`);\n        if (value.content.trim()) {\n          lines.push(value.content.trim());\n        }\n        lines.push(\"\");\n      }\n    }\n    return lines.join(\"\\n\");\n  }\n  saveMemory(name, description, memoryType, content) {\n    if (!MEMORY_TYPES.includes(memoryType)) {\n      return `Error: type must be one of ${MEMORY_TYPES.join(\", \")}`;\n    }\n    const safeName = name.toLowerCase().replace(/[^a-z0-9_-]/gu, \"_\") || \"memory\";\n    fs.mkdirSync(this.memoryDir, { recursive: true });\n    const fileName = `${safeName}.md`;\n    const filePath = path.join(this.memoryDir, fileName);\n    fs.writeFileSync(\n      filePath,\n      `---\nname: ${name}\ndescription: ${description}\ntype: ${memoryType}\n---\n${content}\n`,\n      \"utf8\"\n    );\n    this.memories.set(name, { description, type: memoryType, content, file: fileName });\n    this.rebuildIndex();\n    return `Saved memory '${name}' [${memoryType}] to ${path.relative(WORKDIR, filePath)}`;\n  }\n  rebuildIndex() {\n    fs.mkdirSync(this.memoryDir, { recursive: true });\n    const lines = [\"# Memory Index\", \"\"];\n    for (const [name, value] of this.memories.entries()) {\n      lines.push(`- ${name}: ${value.description} [${value.type}]`);\n    }\n    fs.writeFileSync(path.join(this.memoryDir, \"MEMORY.md\"), `${lines.join(\"\\n\")}\n`, \"utf8\");\n  }\n};\nfunction createSaveMemoryTool(memoryManager2) {\n  return {\n    spec: {\n      name: \"save_memory\",\n      description: \"Save durable cross-session information to the memory store.\",\n      input_schema: {\n        type: \"object\",\n        properties: {\n          name: { type: \"string\" },\n          description: { type: \"string\" },\n          type: { type: \"string\", enum: [...MEMORY_TYPES] },\n          content: { type: \"string\" }\n        },\n        required: [\"name\", \"description\", \"type\", \"content\"]\n      }\n    },\n    execute: (input2) => memoryManager2.saveMemory(\n      String(input2.name || \"\"),\n      String(input2.description || \"\"),\n      String(input2.type || \"project\"),\n      String(input2.content || \"\")\n    )\n  };\n}\nfunction buildPromptSections(sections) {\n  return sections.filter((section) => section.body.trim()).map((section) => `## ${section.title}\n${section.body.trim()}`).join(\"\\n\\n\");\n}\n\n// agents_self_contained/s10_system_prompt.ts\nvar ctx = createLoopContext();\nvar memoryManager = new MemoryManager();\nvar skillRegistry = new SkillRegistry();\nmemoryManager.loadAll();\nvar tools = [...createBaseTools(), createLoadSkillTool(skillRegistry), createSaveMemoryTool(memoryManager)];\nawait runRepl({\n  label: \"s10\",\n  ctx,\n  run: async (loopCtx) => {\n    memoryManager.loadAll();\n    await runAgentLoop(loopCtx, {\n      systemPrompt: buildPromptSections([\n        { title: \"Role\", body: `You are a coding agent at ${WORKDIR}.` },\n        { title: \"Operating Style\", body: \"Prefer tools over prose. Keep outputs concrete and implementation-oriented.\" },\n        { title: \"Skills\", body: skillRegistry.describeAvailable() },\n        { title: \"Memory\", body: memoryManager.loadMemoryPrompt() || \"(no saved memory)\" }\n      ]),\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s11",
      "filename": "s11_error_recovery.ts",
      "title": "Error Recovery",
      "subtitle": "Recover, Then Continue",
      "loc": 404,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file"
      ],
      "newTools": [],
      "coreAddition": "Continuation reasons + retry branches",
      "keyInsight": "Most failures aren't true task failure -- they're signals to try a different path.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        }
      ],
      "layer": "hardening",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s11_error_recovery.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\n\n// agents_self_contained/s11_error_recovery.ts\nvar ctx = createLoopContext();\nvar tools = createBaseTools();\nawait runRepl({\n  label: \"s11\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. If a model call or tool step fails, recover and continue instead of giving up immediately.`,\n      tools,\n      normalizeMessages: true,\n      onModelError: (error, currentCtx, attempt) => {\n        if (attempt > 1) {\n          return false;\n        }\n        const message = error instanceof Error ? error.message : String(error);\n        currentCtx.messages.push({\n          role: \"user\",\n          content: `The previous model call failed with: ${message}. Recover carefully, keep the task state, and continue.`\n        });\n        return true;\n      }\n    });\n  }\n});\n"
    },
    {
      "id": "s12",
      "filename": "s12_task_system.ts",
      "title": "Task System",
      "subtitle": "Durable Work Graph",
      "loc": 518,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task"
      ],
      "newTools": [
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task"
      ],
      "coreAddition": "Task records + dependencies + unlock rules",
      "keyInsight": "Todo lists help a session; durable task graphs coordinate work that outlives it.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTaskBoardTools",
          "signature": "function createTaskBoardTools(taskManager2) {",
          "startLine": 449
        }
      ],
      "layer": "runtime",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s12_task_system.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar TaskManager = class {\n  taskFile;\n  tasks = [];\n  constructor(taskFile = path.join(WORKDIR, \".tasks\", \"tasks.json\")) {\n    this.taskFile = taskFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.taskFile)) {\n      this.tasks = [];\n      return;\n    }\n    this.tasks = JSON.parse(fs.readFileSync(this.taskFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.taskFile), { recursive: true });\n    fs.writeFileSync(this.taskFile, JSON.stringify(this.tasks, null, 2), \"utf8\");\n  }\n  createTask(title, notes = \"\") {\n    const task = {\n      id: cryptoRandomId(),\n      title,\n      status: \"pending\",\n      notes,\n      createdAt: (/* @__PURE__ */ new Date()).toISOString(),\n      updatedAt: (/* @__PURE__ */ new Date()).toISOString()\n    };\n    this.tasks.push(task);\n    this.save();\n    return `Created task ${task.id}: ${task.title}`;\n  }\n  listTasks() {\n    if (this.tasks.length === 0) {\n      return \"No tasks.\";\n    }\n    return this.tasks.map((task) => `${task.id}: [${task.status}] ${task.title}${task.owner ? ` @${task.owner}` : \"\"}`).join(\"\\n\");\n  }\n  updateTask(id, fields) {\n    const task = this.tasks.find((item) => item.id === id);\n    if (!task) {\n      return `Error: Unknown task ${id}`;\n    }\n    Object.assign(task, fields);\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Updated task ${id}`;\n  }\n  claimUnclaimed(agentName) {\n    const task = this.tasks.find((item) => !item.owner && item.status !== \"completed\");\n    if (!task) {\n      return \"No unclaimed tasks.\";\n    }\n    task.owner = agentName;\n    task.status = \"in_progress\";\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Claimed task ${task.id} for ${agentName}`;\n  }\n  scanUnclaimedTasks() {\n    return this.tasks.filter((task) => !task.owner && task.status !== \"completed\");\n  }\n};\nfunction createTaskBoardTools(taskManager2) {\n  return [\n    {\n      spec: {\n        name: \"create_task_record\",\n        description: \"Create a durable task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            title: { type: \"string\" },\n            notes: { type: \"string\" }\n          },\n          required: [\"title\"]\n        }\n      },\n      execute: (input2) => taskManager2.createTask(String(input2.title || \"\"), String(input2.notes || \"\"))\n    },\n    {\n      spec: {\n        name: \"list_task_records\",\n        description: \"List durable task-board items.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => taskManager2.listTasks()\n    },\n    {\n      spec: {\n        name: \"update_task_record\",\n        description: \"Update a task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            id: { type: \"string\" },\n            status: { type: \"string\", enum: [\"pending\", \"in_progress\", \"completed\"] },\n            owner: { type: \"string\" },\n            notes: { type: \"string\" },\n            title: { type: \"string\" }\n          },\n          required: [\"id\"]\n        }\n      },\n      execute: (input2) => taskManager2.updateTask(String(input2.id || \"\"), {\n        status: input2.status,\n        owner: input2.owner ? String(input2.owner) : void 0,\n        notes: input2.notes ? String(input2.notes) : void 0,\n        title: input2.title ? String(input2.title) : void 0\n      })\n    },\n    {\n      spec: {\n        name: \"claim_unclaimed_task\",\n        description: \"Claim the next unclaimed task for an agent.\",\n        input_schema: {\n          type: \"object\",\n          properties: { agent_name: { type: \"string\" } },\n          required: [\"agent_name\"]\n        }\n      },\n      execute: (input2) => taskManager2.claimUnclaimed(String(input2.agent_name || \"agent\"))\n    }\n  ];\n}\n\n// agents_self_contained/s12_task_system.ts\nvar ctx = createLoopContext();\nvar taskManager = new TaskManager();\nvar tools = [...createBaseTools(), ...createTaskBoardTools(taskManager)];\nawait runRepl({\n  label: \"s12\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use the durable task board for work that should survive the current REPL turn.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s13",
      "filename": "s13_background_tasks.ts",
      "title": "Background Tasks",
      "subtitle": "Separate Goal from Running Work",
      "loc": 509,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "background_run",
        "check_background"
      ],
      "newTools": [
        "background_run",
        "check_background"
      ],
      "coreAddition": "RuntimeTaskState + async execution slots",
      "keyInsight": "Background execution is a runtime lane, not a second main loop.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createBackgroundTools",
          "signature": "function createBackgroundTools(backgroundManager2) {",
          "startLine": 466
        }
      ],
      "layer": "runtime",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s13_background_tasks.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar BackgroundManager = class {\n  dir;\n  tasks = {};\n  notifications = [];\n  constructor(dir = path.join(WORKDIR, \".runtime-tasks\")) {\n    this.dir = dir;\n    fs.mkdirSync(this.dir, { recursive: true });\n  }\n  run(command) {\n    const taskId = cryptoRandomId();\n    const outputFile = path.join(this.dir, `${taskId}.log`);\n    this.tasks[taskId] = {\n      status: \"running\",\n      command,\n      result: null,\n      startedAt: Date.now(),\n      outputFile,\n      resultPreview: \"\"\n    };\n    const child = spawn(\"/bin/zsh\", [\"-lc\", command], {\n      cwd: WORKDIR,\n      stdio: [\"ignore\", \"pipe\", \"pipe\"]\n    });\n    let stdout = \"\";\n    let stderr = \"\";\n    child.stdout.on(\"data\", (chunk) => {\n      stdout += String(chunk);\n    });\n    child.stderr.on(\"data\", (chunk) => {\n      stderr += String(chunk);\n    });\n    child.on(\"close\", (code) => {\n      const result = `${stdout}${stderr}`.trim() || \"(no output)\";\n      fs.writeFileSync(outputFile, result, \"utf8\");\n      const preview = result.replace(/\\s+/gu, \" \").slice(0, 500);\n      this.tasks[taskId] = {\n        ...this.tasks[taskId],\n        status: code === 0 ? \"completed\" : \"error\",\n        result,\n        resultPreview: preview\n      };\n      this.notifications.push({\n        taskId,\n        status: this.tasks[taskId].status,\n        preview,\n        outputFile: path.relative(WORKDIR, outputFile)\n      });\n    });\n    return `Background task ${taskId} started: ${command} (output_file=${path.relative(WORKDIR, outputFile)})`;\n  }\n  check(taskId) {\n    if (taskId) {\n      const task = this.tasks[taskId];\n      if (!task) {\n        return `Error: Unknown task ${taskId}`;\n      }\n      if (task.status === \"running\" && !task.result) {\n        return \"[running] (running)\";\n      }\n      return JSON.stringify({\n        id: taskId,\n        status: task.status,\n        command: task.command,\n        result_preview: task.resultPreview,\n        output_file: path.relative(WORKDIR, task.outputFile)\n      }, null, 2);\n    }\n    const entries = Object.entries(this.tasks);\n    if (entries.length === 0) {\n      return \"No background tasks.\";\n    }\n    return entries.map(([id, task]) => `${id}: [${task.status}] ${task.command} -> ${task.resultPreview || \"(running)\"}`).join(\"\\n\");\n  }\n  drainNotifications() {\n    const drained = [...this.notifications];\n    this.notifications = [];\n    return drained;\n  }\n};\nfunction createBackgroundTools(backgroundManager2) {\n  return [\n    {\n      spec: {\n        name: \"background_run\",\n        description: \"Run a slow shell command in the background.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2) => backgroundManager2.run(String(input2.command || \"\"))\n    },\n    {\n      spec: {\n        name: \"check_background\",\n        description: \"Check one background task or list all of them.\",\n        input_schema: {\n          type: \"object\",\n          properties: { task_id: { type: \"string\" } }\n        }\n      },\n      execute: (input2) => backgroundManager2.check(input2.task_id ? String(input2.task_id) : void 0)\n    }\n  ];\n}\n\n// agents_self_contained/s13_background_tasks.ts\nvar ctx = createLoopContext();\nvar backgroundManager = new BackgroundManager();\nvar tools = [...createBaseTools(), ...createBackgroundTools(backgroundManager)];\nawait runRepl({\n  label: \"s13\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use background_run for slow commands and drain notifications before each new model turn.`,\n      tools,\n      normalizeMessages: true,\n      beforeModel: () => {\n        const notifications = backgroundManager.drainNotifications();\n        if (notifications.length > 0) {\n          loopCtx.messages.push({\n            role: \"user\",\n            content: notifications.map((item) => `Background task ${item.taskId} finished [${item.status}] -> ${item.preview} (output_file=${item.outputFile})`).join(\"\\n\")\n          });\n        }\n      }\n    });\n  }\n});\n"
    },
    {
      "id": "s14",
      "filename": "s14_cron_scheduler.ts",
      "title": "Cron Scheduler",
      "subtitle": "Let Time Trigger Work",
      "loc": 603,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "schedule_task",
        "list_schedules",
        "run_due_schedules"
      ],
      "newTools": [
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "schedule_task",
        "list_schedules",
        "run_due_schedules"
      ],
      "coreAddition": "Scheduled triggers over runtime tasks",
      "keyInsight": "Scheduling is not a separate system -- it just feeds the same agent loop from a timer.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTaskBoardTools",
          "signature": "function createTaskBoardTools(taskManager2) {",
          "startLine": 449
        },
        {
          "name": "createCronTools",
          "signature": "function createCronTools(cronScheduler2, taskManager2) {",
          "startLine": 555
        }
      ],
      "layer": "runtime",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s14_cron_scheduler.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar TaskManager = class {\n  taskFile;\n  tasks = [];\n  constructor(taskFile = path.join(WORKDIR, \".tasks\", \"tasks.json\")) {\n    this.taskFile = taskFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.taskFile)) {\n      this.tasks = [];\n      return;\n    }\n    this.tasks = JSON.parse(fs.readFileSync(this.taskFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.taskFile), { recursive: true });\n    fs.writeFileSync(this.taskFile, JSON.stringify(this.tasks, null, 2), \"utf8\");\n  }\n  createTask(title, notes = \"\") {\n    const task = {\n      id: cryptoRandomId(),\n      title,\n      status: \"pending\",\n      notes,\n      createdAt: (/* @__PURE__ */ new Date()).toISOString(),\n      updatedAt: (/* @__PURE__ */ new Date()).toISOString()\n    };\n    this.tasks.push(task);\n    this.save();\n    return `Created task ${task.id}: ${task.title}`;\n  }\n  listTasks() {\n    if (this.tasks.length === 0) {\n      return \"No tasks.\";\n    }\n    return this.tasks.map((task) => `${task.id}: [${task.status}] ${task.title}${task.owner ? ` @${task.owner}` : \"\"}`).join(\"\\n\");\n  }\n  updateTask(id, fields) {\n    const task = this.tasks.find((item) => item.id === id);\n    if (!task) {\n      return `Error: Unknown task ${id}`;\n    }\n    Object.assign(task, fields);\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Updated task ${id}`;\n  }\n  claimUnclaimed(agentName) {\n    const task = this.tasks.find((item) => !item.owner && item.status !== \"completed\");\n    if (!task) {\n      return \"No unclaimed tasks.\";\n    }\n    task.owner = agentName;\n    task.status = \"in_progress\";\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Claimed task ${task.id} for ${agentName}`;\n  }\n  scanUnclaimedTasks() {\n    return this.tasks.filter((task) => !task.owner && task.status !== \"completed\");\n  }\n};\nfunction createTaskBoardTools(taskManager2) {\n  return [\n    {\n      spec: {\n        name: \"create_task_record\",\n        description: \"Create a durable task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            title: { type: \"string\" },\n            notes: { type: \"string\" }\n          },\n          required: [\"title\"]\n        }\n      },\n      execute: (input2) => taskManager2.createTask(String(input2.title || \"\"), String(input2.notes || \"\"))\n    },\n    {\n      spec: {\n        name: \"list_task_records\",\n        description: \"List durable task-board items.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => taskManager2.listTasks()\n    },\n    {\n      spec: {\n        name: \"update_task_record\",\n        description: \"Update a task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            id: { type: \"string\" },\n            status: { type: \"string\", enum: [\"pending\", \"in_progress\", \"completed\"] },\n            owner: { type: \"string\" },\n            notes: { type: \"string\" },\n            title: { type: \"string\" }\n          },\n          required: [\"id\"]\n        }\n      },\n      execute: (input2) => taskManager2.updateTask(String(input2.id || \"\"), {\n        status: input2.status,\n        owner: input2.owner ? String(input2.owner) : void 0,\n        notes: input2.notes ? String(input2.notes) : void 0,\n        title: input2.title ? String(input2.title) : void 0\n      })\n    },\n    {\n      spec: {\n        name: \"claim_unclaimed_task\",\n        description: \"Claim the next unclaimed task for an agent.\",\n        input_schema: {\n          type: \"object\",\n          properties: { agent_name: { type: \"string\" } },\n          required: [\"agent_name\"]\n        }\n      },\n      execute: (input2) => taskManager2.claimUnclaimed(String(input2.agent_name || \"agent\"))\n    }\n  ];\n}\nvar CronScheduler = class {\n  scheduleFile;\n  jobs = [];\n  constructor(scheduleFile = path.join(WORKDIR, \".tasks\", \"cron.json\")) {\n    this.scheduleFile = scheduleFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.scheduleFile)) {\n      this.jobs = [];\n      return;\n    }\n    this.jobs = JSON.parse(fs.readFileSync(this.scheduleFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.scheduleFile), { recursive: true });\n    fs.writeFileSync(this.scheduleFile, JSON.stringify(this.jobs, null, 2), \"utf8\");\n  }\n  schedule(name, everyMinutes, taskTitle) {\n    const job = { id: cryptoRandomId(), name, everyMinutes, taskTitle, lastRunAt: null };\n    this.jobs.push(job);\n    this.save();\n    return `Scheduled ${name} every ${everyMinutes} minutes`;\n  }\n  list() {\n    if (this.jobs.length === 0) {\n      return \"No scheduled jobs.\";\n    }\n    return this.jobs.map((job) => `${job.id}: ${job.name} every ${job.everyMinutes}m -> ${job.taskTitle}`).join(\"\\n\");\n  }\n  runDue(taskManager2) {\n    const now = Date.now();\n    const dueJobs = this.jobs.filter((job) => job.lastRunAt === null || now - job.lastRunAt >= job.everyMinutes * 6e4);\n    if (dueJobs.length === 0) {\n      return \"No jobs are due.\";\n    }\n    for (const job of dueJobs) {\n      taskManager2.createTask(job.taskTitle, `Scheduled by ${job.name}`);\n      job.lastRunAt = now;\n    }\n    this.save();\n    return `Ran ${dueJobs.length} scheduled job(s).`;\n  }\n};\nfunction createCronTools(cronScheduler2, taskManager2) {\n  return [\n    {\n      spec: {\n        name: \"schedule_task\",\n        description: \"Schedule a recurring task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            name: { type: \"string\" },\n            every_minutes: { type: \"integer\" },\n            task_title: { type: \"string\" }\n          },\n          required: [\"name\", \"every_minutes\", \"task_title\"]\n        }\n      },\n      execute: (input2) => cronScheduler2.schedule(\n        String(input2.name || \"\"),\n        Number(input2.every_minutes || 60),\n        String(input2.task_title || \"\")\n      )\n    },\n    {\n      spec: {\n        name: \"list_schedules\",\n        description: \"List recurring schedules.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => cronScheduler2.list()\n    },\n    {\n      spec: {\n        name: \"run_due_schedules\",\n        description: \"Materialize all due scheduled tasks into the task board.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => cronScheduler2.runDue(taskManager2)\n    }\n  ];\n}\n\n// agents_self_contained/s14_cron_scheduler.ts\nvar ctx = createLoopContext();\nvar taskManager = new TaskManager();\nvar cronScheduler = new CronScheduler();\nvar tools = [...createBaseTools(), ...createTaskBoardTools(taskManager), ...createCronTools(cronScheduler, taskManager)];\nawait runRepl({\n  label: \"s14\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use schedules for recurring work and the durable task board for materialized jobs.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s15",
      "filename": "s15_agent_teams.ts",
      "title": "Agent Teams",
      "subtitle": "Persistent Specialists",
      "loc": 509,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "register_teammate",
        "list_teammates",
        "send_team_message",
        "read_team_inbox"
      ],
      "newTools": [
        "register_teammate",
        "list_teammates",
        "send_team_message",
        "read_team_inbox"
      ],
      "coreAddition": "Team roster + teammate lifecycle",
      "keyInsight": "Teammates persist beyond one prompt, have identity, and coordinate through durable channels.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTeamTools",
          "signature": "function createTeamTools(messageBus2, teammateManager2) {",
          "startLine": 442
        }
      ],
      "layer": "platform",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s15_agent_teams.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar MessageBus = class {\n  inboxDir;\n  constructor(inboxDir = path.join(WORKDIR, \".team\", \"inbox\")) {\n    this.inboxDir = inboxDir;\n    fs.mkdirSync(this.inboxDir, { recursive: true });\n  }\n  send(recipient, payload) {\n    const id = cryptoRandomId();\n    const filePath = path.join(this.inboxDir, `${recipient}-${id}.json`);\n    fs.writeFileSync(filePath, JSON.stringify({ id, recipient, ...payload }, null, 2), \"utf8\");\n    return `Sent message ${id} to ${recipient}`;\n  }\n  read(recipient) {\n    const files = fs.readdirSync(this.inboxDir).filter((fileName) => fileName.endsWith(\".json\"));\n    const relevant = recipient ? files.filter((fileName) => fileName.startsWith(`${recipient}-`)) : files;\n    if (relevant.length === 0) {\n      return \"Inbox is empty.\";\n    }\n    return relevant.map((fileName) => {\n      const payload = JSON.parse(fs.readFileSync(path.join(this.inboxDir, fileName), \"utf8\"));\n      return `${payload.id}: ${payload.type || \"message\"} -> ${payload.subject || \"(no subject)\"}`;\n    }).join(\"\\n\");\n  }\n};\nvar TeammateManager = class {\n  teammateFile;\n  teammates = [];\n  constructor(teammateFile = path.join(WORKDIR, \".team\", \"teammates.json\")) {\n    this.teammateFile = teammateFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.teammateFile)) {\n      this.teammates = [];\n      return;\n    }\n    this.teammates = JSON.parse(fs.readFileSync(this.teammateFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.teammateFile), { recursive: true });\n    fs.writeFileSync(this.teammateFile, JSON.stringify(this.teammates, null, 2), \"utf8\");\n  }\n  register(name, role) {\n    this.teammates = this.teammates.filter((teammate) => teammate.name !== name);\n    this.teammates.push({ name, role });\n    this.save();\n    return `Registered teammate ${name} (${role})`;\n  }\n  list() {\n    if (this.teammates.length === 0) {\n      return \"No teammates registered.\";\n    }\n    return this.teammates.map((teammate) => `- ${teammate.name}: ${teammate.role}`).join(\"\\n\");\n  }\n};\nfunction createTeamTools(messageBus2, teammateManager2) {\n  return [\n    {\n      spec: {\n        name: \"register_teammate\",\n        description: \"Register or update a teammate profile.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            name: { type: \"string\" },\n            role: { type: \"string\" }\n          },\n          required: [\"name\", \"role\"]\n        }\n      },\n      execute: (input2) => teammateManager2.register(String(input2.name || \"\"), String(input2.role || \"\"))\n    },\n    {\n      spec: {\n        name: \"list_teammates\",\n        description: \"List registered teammates.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => teammateManager2.list()\n    },\n    {\n      spec: {\n        name: \"send_team_message\",\n        description: \"Send a message into the shared team inbox.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            recipient: { type: \"string\" },\n            type: { type: \"string\" },\n            subject: { type: \"string\" },\n            body: { type: \"string\" }\n          },\n          required: [\"recipient\", \"type\", \"subject\", \"body\"]\n        }\n      },\n      execute: (input2) => messageBus2.send(String(input2.recipient || \"\"), {\n        type: String(input2.type || \"message\"),\n        subject: String(input2.subject || \"\"),\n        body: String(input2.body || \"\")\n      })\n    },\n    {\n      spec: {\n        name: \"read_team_inbox\",\n        description: \"Read inbox messages for one teammate or for everyone.\",\n        input_schema: {\n          type: \"object\",\n          properties: { recipient: { type: \"string\" } }\n        }\n      },\n      execute: (input2) => messageBus2.read(input2.recipient ? String(input2.recipient) : void 0)\n    }\n  ];\n}\n\n// agents_self_contained/s15_agent_teams.ts\nvar ctx = createLoopContext();\nvar messageBus = new MessageBus();\nvar teammateManager = new TeammateManager();\nvar tools = [...createBaseTools(), ...createTeamTools(messageBus, teammateManager)];\nawait runRepl({\n  label: \"s15\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use the team inbox to coordinate with persistent teammates.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s16",
      "filename": "s16_team_protocols.ts",
      "title": "Team Protocols",
      "subtitle": "Shared Request-Response Rules",
      "loc": 557,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "register_teammate",
        "list_teammates",
        "send_team_message",
        "read_team_inbox",
        "request_plan_approval",
        "respond_to_request"
      ],
      "newTools": [
        "request_plan_approval",
        "respond_to_request"
      ],
      "coreAddition": "Protocol envelopes + request correlation",
      "keyInsight": "A protocol request is a structured message with an ID; the response must reference the same ID.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTeamTools",
          "signature": "function createTeamTools(messageBus2, teammateManager2) {",
          "startLine": 442
        },
        {
          "name": "createProtocolTools",
          "signature": "function createProtocolTools(messageBus2) {",
          "startLine": 501
        }
      ],
      "layer": "platform",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s16_team_protocols.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar MessageBus = class {\n  inboxDir;\n  constructor(inboxDir = path.join(WORKDIR, \".team\", \"inbox\")) {\n    this.inboxDir = inboxDir;\n    fs.mkdirSync(this.inboxDir, { recursive: true });\n  }\n  send(recipient, payload) {\n    const id = cryptoRandomId();\n    const filePath = path.join(this.inboxDir, `${recipient}-${id}.json`);\n    fs.writeFileSync(filePath, JSON.stringify({ id, recipient, ...payload }, null, 2), \"utf8\");\n    return `Sent message ${id} to ${recipient}`;\n  }\n  read(recipient) {\n    const files = fs.readdirSync(this.inboxDir).filter((fileName) => fileName.endsWith(\".json\"));\n    const relevant = recipient ? files.filter((fileName) => fileName.startsWith(`${recipient}-`)) : files;\n    if (relevant.length === 0) {\n      return \"Inbox is empty.\";\n    }\n    return relevant.map((fileName) => {\n      const payload = JSON.parse(fs.readFileSync(path.join(this.inboxDir, fileName), \"utf8\"));\n      return `${payload.id}: ${payload.type || \"message\"} -> ${payload.subject || \"(no subject)\"}`;\n    }).join(\"\\n\");\n  }\n};\nvar TeammateManager = class {\n  teammateFile;\n  teammates = [];\n  constructor(teammateFile = path.join(WORKDIR, \".team\", \"teammates.json\")) {\n    this.teammateFile = teammateFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.teammateFile)) {\n      this.teammates = [];\n      return;\n    }\n    this.teammates = JSON.parse(fs.readFileSync(this.teammateFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.teammateFile), { recursive: true });\n    fs.writeFileSync(this.teammateFile, JSON.stringify(this.teammates, null, 2), \"utf8\");\n  }\n  register(name, role) {\n    this.teammates = this.teammates.filter((teammate) => teammate.name !== name);\n    this.teammates.push({ name, role });\n    this.save();\n    return `Registered teammate ${name} (${role})`;\n  }\n  list() {\n    if (this.teammates.length === 0) {\n      return \"No teammates registered.\";\n    }\n    return this.teammates.map((teammate) => `- ${teammate.name}: ${teammate.role}`).join(\"\\n\");\n  }\n};\nfunction createTeamTools(messageBus2, teammateManager2) {\n  return [\n    {\n      spec: {\n        name: \"register_teammate\",\n        description: \"Register or update a teammate profile.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            name: { type: \"string\" },\n            role: { type: \"string\" }\n          },\n          required: [\"name\", \"role\"]\n        }\n      },\n      execute: (input2) => teammateManager2.register(String(input2.name || \"\"), String(input2.role || \"\"))\n    },\n    {\n      spec: {\n        name: \"list_teammates\",\n        description: \"List registered teammates.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => teammateManager2.list()\n    },\n    {\n      spec: {\n        name: \"send_team_message\",\n        description: \"Send a message into the shared team inbox.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            recipient: { type: \"string\" },\n            type: { type: \"string\" },\n            subject: { type: \"string\" },\n            body: { type: \"string\" }\n          },\n          required: [\"recipient\", \"type\", \"subject\", \"body\"]\n        }\n      },\n      execute: (input2) => messageBus2.send(String(input2.recipient || \"\"), {\n        type: String(input2.type || \"message\"),\n        subject: String(input2.subject || \"\"),\n        body: String(input2.body || \"\")\n      })\n    },\n    {\n      spec: {\n        name: \"read_team_inbox\",\n        description: \"Read inbox messages for one teammate or for everyone.\",\n        input_schema: {\n          type: \"object\",\n          properties: { recipient: { type: \"string\" } }\n        }\n      },\n      execute: (input2) => messageBus2.read(input2.recipient ? String(input2.recipient) : void 0)\n    }\n  ];\n}\nfunction createProtocolTools(messageBus2) {\n  return [\n    {\n      spec: {\n        name: \"request_plan_approval\",\n        description: \"Send a structured plan approval request to another teammate.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            recipient: { type: \"string\" },\n            subject: { type: \"string\" },\n            plan: { type: \"string\" }\n          },\n          required: [\"recipient\", \"subject\", \"plan\"]\n        }\n      },\n      execute: (input2) => messageBus2.send(String(input2.recipient || \"\"), {\n        type: \"plan_approval_request\",\n        subject: String(input2.subject || \"\"),\n        body: String(input2.plan || \"\")\n      })\n    },\n    {\n      spec: {\n        name: \"respond_to_request\",\n        description: \"Respond to a structured request in the team inbox.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            recipient: { type: \"string\" },\n            approved: { type: \"boolean\" },\n            response: { type: \"string\" }\n          },\n          required: [\"recipient\", \"approved\", \"response\"]\n        }\n      },\n      execute: (input2) => messageBus2.send(String(input2.recipient || \"\"), {\n        type: \"plan_approval_response\",\n        approved: Boolean(input2.approved),\n        body: String(input2.response || \"\")\n      })\n    }\n  ];\n}\n\n// agents_self_contained/s16_team_protocols.ts\nvar ctx = createLoopContext();\nvar messageBus = new MessageBus();\nvar teammateManager = new TeammateManager();\nvar tools = [\n  ...createBaseTools(),\n  ...createTeamTools(messageBus, teammateManager),\n  ...createProtocolTools(messageBus)\n];\nawait runRepl({\n  label: \"s16\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use structured team protocols for plan approvals, requests, and responses.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s17",
      "filename": "s17_autonomous_agents.ts",
      "title": "Autonomous Agents",
      "subtitle": "Self-Claim and Self-Resume",
      "loc": 532,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "scan_unclaimed_tasks"
      ],
      "newTools": [
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "scan_unclaimed_tasks"
      ],
      "coreAddition": "Idle polling + role-aware self-claim + resume context",
      "keyInsight": "Autonomy is a bounded mechanism -- idle, scan, claim, resume -- not magic.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTaskBoardTools",
          "signature": "function createTaskBoardTools(taskManager2) {",
          "startLine": 449
        }
      ],
      "layer": "platform",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s17_autonomous_agents.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar TaskManager = class {\n  taskFile;\n  tasks = [];\n  constructor(taskFile = path.join(WORKDIR, \".tasks\", \"tasks.json\")) {\n    this.taskFile = taskFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.taskFile)) {\n      this.tasks = [];\n      return;\n    }\n    this.tasks = JSON.parse(fs.readFileSync(this.taskFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.taskFile), { recursive: true });\n    fs.writeFileSync(this.taskFile, JSON.stringify(this.tasks, null, 2), \"utf8\");\n  }\n  createTask(title, notes = \"\") {\n    const task = {\n      id: cryptoRandomId(),\n      title,\n      status: \"pending\",\n      notes,\n      createdAt: (/* @__PURE__ */ new Date()).toISOString(),\n      updatedAt: (/* @__PURE__ */ new Date()).toISOString()\n    };\n    this.tasks.push(task);\n    this.save();\n    return `Created task ${task.id}: ${task.title}`;\n  }\n  listTasks() {\n    if (this.tasks.length === 0) {\n      return \"No tasks.\";\n    }\n    return this.tasks.map((task) => `${task.id}: [${task.status}] ${task.title}${task.owner ? ` @${task.owner}` : \"\"}`).join(\"\\n\");\n  }\n  updateTask(id, fields) {\n    const task = this.tasks.find((item) => item.id === id);\n    if (!task) {\n      return `Error: Unknown task ${id}`;\n    }\n    Object.assign(task, fields);\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Updated task ${id}`;\n  }\n  claimUnclaimed(agentName) {\n    const task = this.tasks.find((item) => !item.owner && item.status !== \"completed\");\n    if (!task) {\n      return \"No unclaimed tasks.\";\n    }\n    task.owner = agentName;\n    task.status = \"in_progress\";\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Claimed task ${task.id} for ${agentName}`;\n  }\n  scanUnclaimedTasks() {\n    return this.tasks.filter((task) => !task.owner && task.status !== \"completed\");\n  }\n};\nfunction createTaskBoardTools(taskManager2) {\n  return [\n    {\n      spec: {\n        name: \"create_task_record\",\n        description: \"Create a durable task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            title: { type: \"string\" },\n            notes: { type: \"string\" }\n          },\n          required: [\"title\"]\n        }\n      },\n      execute: (input2) => taskManager2.createTask(String(input2.title || \"\"), String(input2.notes || \"\"))\n    },\n    {\n      spec: {\n        name: \"list_task_records\",\n        description: \"List durable task-board items.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => taskManager2.listTasks()\n    },\n    {\n      spec: {\n        name: \"update_task_record\",\n        description: \"Update a task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            id: { type: \"string\" },\n            status: { type: \"string\", enum: [\"pending\", \"in_progress\", \"completed\"] },\n            owner: { type: \"string\" },\n            notes: { type: \"string\" },\n            title: { type: \"string\" }\n          },\n          required: [\"id\"]\n        }\n      },\n      execute: (input2) => taskManager2.updateTask(String(input2.id || \"\"), {\n        status: input2.status,\n        owner: input2.owner ? String(input2.owner) : void 0,\n        notes: input2.notes ? String(input2.notes) : void 0,\n        title: input2.title ? String(input2.title) : void 0\n      })\n    },\n    {\n      spec: {\n        name: \"claim_unclaimed_task\",\n        description: \"Claim the next unclaimed task for an agent.\",\n        input_schema: {\n          type: \"object\",\n          properties: { agent_name: { type: \"string\" } },\n          required: [\"agent_name\"]\n        }\n      },\n      execute: (input2) => taskManager2.claimUnclaimed(String(input2.agent_name || \"agent\"))\n    }\n  ];\n}\n\n// agents_self_contained/s17_autonomous_agents.ts\nvar ctx = createLoopContext();\nvar taskManager = new TaskManager();\nvar tools = [\n  ...createBaseTools(),\n  ...createTaskBoardTools(taskManager),\n  {\n    spec: {\n      name: \"scan_unclaimed_tasks\",\n      description: \"List durable tasks that are not owned by any agent.\",\n      input_schema: { type: \"object\", properties: {} }\n    },\n    execute: () => {\n      const tasks = taskManager.scanUnclaimedTasks();\n      return tasks.length > 0 ? tasks.map((task) => `${task.id}: [${task.status}] ${task.title}`).join(\"\\n\") : \"No unclaimed tasks.\";\n    }\n  }\n];\nawait runRepl({\n  label: \"s17\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Autonomous agents can scan for unclaimed work and claim it without waiting for a direct prompt.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s18",
      "filename": "s18_worktree_task_isolation.ts",
      "title": "Worktree Isolation",
      "subtitle": "Separate Directory, Separate Lane",
      "loc": 581,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "create_lane",
        "list_lanes",
        "run_in_lane"
      ],
      "newTools": [
        "create_lane",
        "list_lanes",
        "run_in_lane"
      ],
      "coreAddition": "Task-worktree state + explicit enter/closeout lifecycle",
      "keyInsight": "Tasks answer what; worktrees answer where. Keep them separate.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createTaskBoardTools",
          "signature": "function createTaskBoardTools(taskManager2) {",
          "startLine": 449
        },
        {
          "name": "createWorktreeTools",
          "signature": "function createWorktreeTools(worktreeManager2) {",
          "startLine": 534
        }
      ],
      "layer": "platform",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s18_worktree_task_isolation.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar TaskManager = class {\n  taskFile;\n  tasks = [];\n  constructor(taskFile = path.join(WORKDIR, \".tasks\", \"tasks.json\")) {\n    this.taskFile = taskFile;\n    this.load();\n  }\n  load() {\n    if (!fs.existsSync(this.taskFile)) {\n      this.tasks = [];\n      return;\n    }\n    this.tasks = JSON.parse(fs.readFileSync(this.taskFile, \"utf8\"));\n  }\n  save() {\n    fs.mkdirSync(path.dirname(this.taskFile), { recursive: true });\n    fs.writeFileSync(this.taskFile, JSON.stringify(this.tasks, null, 2), \"utf8\");\n  }\n  createTask(title, notes = \"\") {\n    const task = {\n      id: cryptoRandomId(),\n      title,\n      status: \"pending\",\n      notes,\n      createdAt: (/* @__PURE__ */ new Date()).toISOString(),\n      updatedAt: (/* @__PURE__ */ new Date()).toISOString()\n    };\n    this.tasks.push(task);\n    this.save();\n    return `Created task ${task.id}: ${task.title}`;\n  }\n  listTasks() {\n    if (this.tasks.length === 0) {\n      return \"No tasks.\";\n    }\n    return this.tasks.map((task) => `${task.id}: [${task.status}] ${task.title}${task.owner ? ` @${task.owner}` : \"\"}`).join(\"\\n\");\n  }\n  updateTask(id, fields) {\n    const task = this.tasks.find((item) => item.id === id);\n    if (!task) {\n      return `Error: Unknown task ${id}`;\n    }\n    Object.assign(task, fields);\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Updated task ${id}`;\n  }\n  claimUnclaimed(agentName) {\n    const task = this.tasks.find((item) => !item.owner && item.status !== \"completed\");\n    if (!task) {\n      return \"No unclaimed tasks.\";\n    }\n    task.owner = agentName;\n    task.status = \"in_progress\";\n    task.updatedAt = (/* @__PURE__ */ new Date()).toISOString();\n    this.save();\n    return `Claimed task ${task.id} for ${agentName}`;\n  }\n  scanUnclaimedTasks() {\n    return this.tasks.filter((task) => !task.owner && task.status !== \"completed\");\n  }\n};\nfunction createTaskBoardTools(taskManager2) {\n  return [\n    {\n      spec: {\n        name: \"create_task_record\",\n        description: \"Create a durable task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            title: { type: \"string\" },\n            notes: { type: \"string\" }\n          },\n          required: [\"title\"]\n        }\n      },\n      execute: (input2) => taskManager2.createTask(String(input2.title || \"\"), String(input2.notes || \"\"))\n    },\n    {\n      spec: {\n        name: \"list_task_records\",\n        description: \"List durable task-board items.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => taskManager2.listTasks()\n    },\n    {\n      spec: {\n        name: \"update_task_record\",\n        description: \"Update a task-board item.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            id: { type: \"string\" },\n            status: { type: \"string\", enum: [\"pending\", \"in_progress\", \"completed\"] },\n            owner: { type: \"string\" },\n            notes: { type: \"string\" },\n            title: { type: \"string\" }\n          },\n          required: [\"id\"]\n        }\n      },\n      execute: (input2) => taskManager2.updateTask(String(input2.id || \"\"), {\n        status: input2.status,\n        owner: input2.owner ? String(input2.owner) : void 0,\n        notes: input2.notes ? String(input2.notes) : void 0,\n        title: input2.title ? String(input2.title) : void 0\n      })\n    },\n    {\n      spec: {\n        name: \"claim_unclaimed_task\",\n        description: \"Claim the next unclaimed task for an agent.\",\n        input_schema: {\n          type: \"object\",\n          properties: { agent_name: { type: \"string\" } },\n          required: [\"agent_name\"]\n        }\n      },\n      execute: (input2) => taskManager2.claimUnclaimed(String(input2.agent_name || \"agent\"))\n    }\n  ];\n}\nvar WorktreeManager = class {\n  rootDir;\n  constructor(rootDir = path.join(WORKDIR, \".worktrees\")) {\n    this.rootDir = rootDir;\n    fs.mkdirSync(this.rootDir, { recursive: true });\n  }\n  createLane(name) {\n    const lanePath = path.join(this.rootDir, name);\n    fs.mkdirSync(lanePath, { recursive: true });\n    return `Created isolated lane at ${path.relative(WORKDIR, lanePath)}`;\n  }\n  listLanes() {\n    const lanes = fs.readdirSync(this.rootDir, { withFileTypes: true }).filter((entry) => entry.isDirectory()).map((entry) => entry.name);\n    return lanes.length > 0 ? lanes.map((lane) => `- ${lane}`).join(\"\\n\") : \"No isolated lanes.\";\n  }\n  runInLane(name, command) {\n    const lanePath = path.join(this.rootDir, name);\n    if (!fs.existsSync(lanePath)) {\n      return `Error: Unknown lane ${name}`;\n    }\n    return runBash(command, lanePath);\n  }\n};\nfunction createWorktreeTools(worktreeManager2) {\n  return [\n    {\n      spec: {\n        name: \"create_lane\",\n        description: \"Create an isolated execution lane for a task.\",\n        input_schema: {\n          type: \"object\",\n          properties: { name: { type: \"string\" } },\n          required: [\"name\"]\n        }\n      },\n      execute: (input2) => worktreeManager2.createLane(String(input2.name || \"lane\"))\n    },\n    {\n      spec: {\n        name: \"list_lanes\",\n        description: \"List isolated execution lanes.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => worktreeManager2.listLanes()\n    },\n    {\n      spec: {\n        name: \"run_in_lane\",\n        description: \"Run a shell command inside an isolated lane.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            name: { type: \"string\" },\n            command: { type: \"string\" }\n          },\n          required: [\"name\", \"command\"]\n        }\n      },\n      execute: (input2) => worktreeManager2.runInLane(String(input2.name || \"\"), String(input2.command || \"\"))\n    }\n  ];\n}\n\n// agents_self_contained/s18_worktree_task_isolation.ts\nvar ctx = createLoopContext();\nvar taskManager = new TaskManager();\nvar worktreeManager = new WorktreeManager();\nvar tools = [...createBaseTools(), ...createTaskBoardTools(taskManager), ...createWorktreeTools(worktreeManager)];\nawait runRepl({\n  label: \"s18\",\n  ctx,\n  run: async (loopCtx) => {\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. Use isolated lanes when parallel work would otherwise trample shared state.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    },
    {
      "id": "s19",
      "filename": "s19_mcp_plugin.ts",
      "title": "MCP & Plugin",
      "subtitle": "External Capability Bus",
      "loc": 453,
      "tools": [
        "bash",
        "read_file",
        "write_file",
        "edit_file",
        "list_plugins",
        "describe_plugin"
      ],
      "newTools": [
        "list_plugins",
        "describe_plugin"
      ],
      "coreAddition": "Scoped servers + capability routing",
      "keyInsight": "External capabilities join the same routing, permission, and result-append path as native tools.",
      "classes": [],
      "functions": [
        {
          "name": "getModelId",
          "signature": "function getModelId() {",
          "startLine": 18
        },
        {
          "name": "getAnthropicClient",
          "signature": "function getAnthropicClient() {",
          "startLine": 21
        },
        {
          "name": "createLoopContext",
          "signature": "function createLoopContext() {",
          "startLine": 31
        },
        {
          "name": "safePath",
          "signature": "function safePath(relativePath) {",
          "startLine": 34
        },
        {
          "name": "runBash",
          "signature": "function runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {",
          "startLine": 42
        },
        {
          "name": "readWorkspaceFile",
          "signature": "function readWorkspaceFile(filePath, limit) {",
          "startLine": 69
        },
        {
          "name": "writeWorkspaceFile",
          "signature": "function writeWorkspaceFile(filePath, content) {",
          "startLine": 82
        },
        {
          "name": "editWorkspaceFile",
          "signature": "function editWorkspaceFile(filePath, oldText, newText) {",
          "startLine": 92
        },
        {
          "name": "extractText",
          "signature": "function extractText(content) {",
          "startLine": 107
        },
        {
          "name": "normalizeContent",
          "signature": "function normalizeContent(content) {",
          "startLine": 121
        },
        {
          "name": "normalizeMessages",
          "signature": "function normalizeMessages(messages) {",
          "startLine": 132
        },
        {
          "name": "callModel",
          "signature": "async function callModel(systemPrompt, messages, tools2) {",
          "startLine": 174
        },
        {
          "name": "runAgentLoop",
          "signature": "async function runAgentLoop(ctx2, config) {",
          "startLine": 184
        },
        {
          "name": "runRepl",
          "signature": "async function runRepl(options) {",
          "startLine": 236
        },
        {
          "name": "cryptoRandomId",
          "signature": "function cryptoRandomId() {",
          "startLine": 260
        },
        {
          "name": "withGuards",
          "signature": "function withGuards(toolName, input2, action, options) {",
          "startLine": 263
        },
        {
          "name": "createBaseTools",
          "signature": "function createBaseTools(options) {",
          "startLine": 297
        },
        {
          "name": "createPluginTools",
          "signature": "function createPluginTools(pluginLoader2) {",
          "startLine": 421
        }
      ],
      "layer": "platform",
      "source": "// @ts-nocheck\n// AUTO-GENERATED by scripts/generate-self-contained-agents.mjs\n// Source chapter: agents_self_contained/s19_mcp_plugin.ts\n// Tree-shaken bundle: chapter wiring + only used runtime code.\n// agents_self_contained/_runtime.ts\nimport Anthropic from \"@anthropic-ai/sdk\";\nimport dotenv from \"dotenv\";\nimport { execSync, spawn, spawnSync } from \"node:child_process\";\nimport fs from \"node:fs\";\nimport path from \"node:path\";\nimport process from \"node:process\";\nimport readline from \"node:readline/promises\";\nimport { stdin as input, stdout as output } from \"node:process\";\ndotenv.config({ override: true });\nvar WORKDIR = process.cwd();\nvar DEFAULT_MODEL = \"claude-3-5-sonnet-latest\";\nvar anthropicClient = null;\nfunction getModelId() {\n  return process.env.MODEL_ID || DEFAULT_MODEL;\n}\nfunction getAnthropicClient() {\n  if (anthropicClient) {\n    return anthropicClient;\n  }\n  anthropicClient = new Anthropic({\n    apiKey: process.env.ANTHROPIC_API_KEY || process.env.ANTHROPIC_AUTH_TOKEN || \"missing-api-key\",\n    baseURL: process.env.ANTHROPIC_BASE_URL || void 0\n  });\n  return anthropicClient;\n}\nfunction createLoopContext() {\n  return { workdir: WORKDIR, messages: [], meta: {} };\n}\nfunction safePath(relativePath) {\n  const resolved = path.resolve(WORKDIR, relativePath);\n  const relative = path.relative(WORKDIR, resolved);\n  if (relative.startsWith(\"..\") || path.isAbsolute(relative)) {\n    throw new Error(`Path escapes workspace: ${relativePath}`);\n  }\n  return resolved;\n}\nfunction runBash(command, cwd = WORKDIR, timeoutMs = 12e4) {\n  const dangerous = [\"rm -rf /\", \"sudo \", \"shutdown\", \"reboot\", \"> /dev/\"];\n  if (dangerous.some((item) => command.includes(item))) {\n    return \"Error: Dangerous command blocked\";\n  }\n  try {\n    const output2 = execSync(command, {\n      cwd,\n      encoding: \"utf8\",\n      timeout: timeoutMs,\n      maxBuffer: 10 * 1024 * 1024,\n      shell: \"/bin/zsh\"\n    });\n    return output2.trim() || \"(no output)\";\n  } catch (error) {\n    const stdout = typeof error === \"object\" && error !== null && \"stdout\" in error ? String(error.stdout || \"\") : \"\";\n    const stderr = typeof error === \"object\" && error !== null && \"stderr\" in error ? String(error.stderr || \"\") : \"\";\n    const message = `${stdout}${stderr}`.trim();\n    if (message) {\n      return message.slice(0, 5e4);\n    }\n    if (error instanceof Error) {\n      return `Error: ${error.message}`;\n    }\n    return \"Error: Unknown shell failure\";\n  }\n}\nfunction readWorkspaceFile(filePath, limit) {\n  try {\n    const text = fs.readFileSync(safePath(filePath), \"utf8\");\n    const lines = text.split(/\\r?\\n/u);\n    if (typeof limit === \"number\" && limit > 0 && lines.length > limit) {\n      return `${lines.slice(0, limit).join(\"\\n\")}\n... (${lines.length - limit} more lines)`;\n    }\n    return text.slice(0, 5e4);\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction writeWorkspaceFile(filePath, content) {\n  try {\n    const resolved = safePath(filePath);\n    fs.mkdirSync(path.dirname(resolved), { recursive: true });\n    fs.writeFileSync(resolved, content, \"utf8\");\n    return `Wrote ${Buffer.byteLength(content, \"utf8\")} bytes to ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction editWorkspaceFile(filePath, oldText, newText) {\n  try {\n    const resolved = safePath(filePath);\n    const content = fs.readFileSync(resolved, \"utf8\");\n    const index = content.indexOf(oldText);\n    if (index === -1) {\n      return `Error: Text not found in ${filePath}`;\n    }\n    const updated = `${content.slice(0, index)}${newText}${content.slice(index + oldText.length)}`;\n    fs.writeFileSync(resolved, updated, \"utf8\");\n    return `Edited ${filePath}`;\n  } catch (error) {\n    return `Error: ${error instanceof Error ? error.message : String(error)}`;\n  }\n}\nfunction extractText(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.map((block) => {\n    if (typeof block === \"object\" && block !== null && \"text\" in block && typeof block.text === \"string\") {\n      return block.text;\n    }\n    if (typeof block === \"object\" && block !== null && \"content\" in block && typeof block.content === \"string\") {\n      return block.content;\n    }\n    return \"\";\n  }).filter(Boolean).join(\"\\n\").trim();\n}\nfunction normalizeContent(content) {\n  if (typeof content === \"string\") {\n    return content;\n  }\n  return content.filter((block) => typeof block === \"object\" && block !== null).map((block) => {\n    const cleaned = Object.fromEntries(\n      Object.entries(block).filter(([key]) => !key.startsWith(\"_\"))\n    );\n    return cleaned;\n  });\n}\nfunction normalizeMessages(messages) {\n  const cleaned = messages.map((message) => ({\n    role: message.role,\n    content: normalizeContent(message.content)\n  }));\n  const existingToolResults = /* @__PURE__ */ new Set();\n  for (const message of cleaned) {\n    if (!Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_result\" && typeof block.tool_use_id === \"string\") {\n        existingToolResults.add(block.tool_use_id);\n      }\n    }\n  }\n  for (const message of cleaned) {\n    if (message.role !== \"assistant\" || !Array.isArray(message.content)) {\n      continue;\n    }\n    for (const block of message.content) {\n      if (typeof block === \"object\" && block !== null && block.type === \"tool_use\" && typeof block.id === \"string\" && !existingToolResults.has(block.id)) {\n        cleaned.push({\n          role: \"user\",\n          content: [{ type: \"tool_result\", tool_use_id: block.id, content: \"(cancelled)\" }]\n        });\n      }\n    }\n  }\n  const merged = [];\n  for (const message of cleaned) {\n    const previous = merged.at(-1);\n    if (!previous || previous.role !== message.role) {\n      merged.push(message);\n      continue;\n    }\n    const previousBlocks = typeof previous.content === \"string\" ? [{ type: \"text\", text: previous.content }] : previous.content;\n    const nextBlocks = typeof message.content === \"string\" ? [{ type: \"text\", text: message.content }] : message.content;\n    previous.content = [...previousBlocks, ...nextBlocks];\n  }\n  return merged;\n}\nasync function callModel(systemPrompt, messages, tools2) {\n  const client = getAnthropicClient();\n  return client.messages.create({\n    model: getModelId(),\n    system: systemPrompt,\n    messages,\n    tools: tools2,\n    max_tokens: 8e3\n  });\n}\nasync function runAgentLoop(ctx2, config) {\n  const maxTurns = config.maxTurns ?? 30;\n  const toolMap = new Map(config.tools.map((tool) => [tool.spec.name, tool]));\n  for (let turn = 0; turn < maxTurns; turn += 1) {\n    if (config.beforeModel) {\n      await config.beforeModel(ctx2);\n    }\n    const systemPrompt = typeof config.systemPrompt === \"function\" ? config.systemPrompt() : config.systemPrompt;\n    const apiMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n    let response;\n    let modelAttempt = 0;\n    for (; ; ) {\n      try {\n        const retryMessages = config.normalizeMessages ? normalizeMessages(ctx2.messages) : ctx2.messages;\n        response = await callModel(systemPrompt, retryMessages, config.tools.map((tool) => tool.spec));\n        break;\n      } catch (error) {\n        modelAttempt += 1;\n        const shouldRetry = config.onModelError ? await config.onModelError(error, ctx2, modelAttempt) : false;\n        if (!shouldRetry) {\n          throw error;\n        }\n      }\n    }\n    ctx2.messages.push({ role: \"assistant\", content: response.content });\n    if (response.stop_reason !== \"tool_use\") {\n      return;\n    }\n    const toolResults = [];\n    for (const block of response.content) {\n      if (block.type !== \"tool_use\") {\n        continue;\n      }\n      const registered = toolMap.get(block.name);\n      const toolUseId = String(block.id || cryptoRandomId());\n      const input2 = typeof block.input === \"object\" && block.input !== null ? block.input : {};\n      let outputText;\n      if (!registered) {\n        outputText = `Unknown tool: ${block.name}`;\n      } else {\n        outputText = await registered.execute(input2, ctx2, toolUseId);\n      }\n      console.log(`> ${block.name}`);\n      console.log(outputText.slice(0, 200));\n      toolResults.push({ type: \"tool_result\", tool_use_id: toolUseId, content: outputText });\n    }\n    ctx2.messages.push({ role: \"user\", content: toolResults });\n    if (config.afterToolResults) {\n      await config.afterToolResults(ctx2);\n    }\n  }\n}\nasync function runRepl(options) {\n  const rl = readline.createInterface({ input, output });\n  try {\n    if (options.onSessionStart) {\n      await options.onSessionStart(options.ctx);\n    }\n    for (; ; ) {\n      const query = await rl.question(`\\x1B[36m${options.label} >> \\x1B[0m`);\n      if (!query.trim() || [\"q\", \"exit\"].includes(query.trim().toLowerCase())) {\n        break;\n      }\n      options.ctx.messages.push({ role: \"user\", content: query });\n      await options.run(options.ctx);\n      const lastAssistant = [...options.ctx.messages].reverse().find((message) => message.role === \"assistant\");\n      const finalText = lastAssistant ? extractText(lastAssistant.content) : \"\";\n      if (finalText) {\n        console.log(finalText);\n      }\n      console.log(\"\");\n    }\n  } finally {\n    rl.close();\n  }\n}\nfunction cryptoRandomId() {\n  return Math.random().toString(36).slice(2, 10);\n}\nfunction withGuards(toolName, input2, action, options) {\n  const hookContext = { tool_name: toolName, tool_input: input2 };\n  if (options?.permissions) {\n    const decision = options.permissions.check(toolName, input2);\n    if (decision.behavior === \"deny\") {\n      return `Permission denied: ${decision.reason}`;\n    }\n    if (decision.behavior === \"ask\" && !options.permissions.askUser(toolName, input2)) {\n      return `Permission denied by user for ${toolName}`;\n    }\n  }\n  if (options?.hooks) {\n    const pre = options.hooks.runHooks(\"PreToolUse\", hookContext);\n    if (pre.blocked) {\n      return `Blocked by hook: ${pre.blockReason || \"unknown\"}`;\n    }\n  }\n  let outputText = action();\n  if (toolName === \"read_file\" && typeof input2.path === \"string\" && options?.onReadPath) {\n    options.onReadPath(input2.path);\n  }\n  if (options?.compact) {\n    outputText = options.compact.persistLargeOutput(options.toolUseId || cryptoRandomId(), outputText);\n  }\n  if (options?.hooks) {\n    const post = options.hooks.runHooks(\"PostToolUse\", { ...hookContext, tool_output: outputText });\n    if (post.messages.length > 0) {\n      outputText = `${outputText}\n\n${post.messages.join(\"\\n\")}`;\n    }\n  }\n  return outputText.slice(0, 5e4);\n}\nfunction createBaseTools(options) {\n  return [\n    {\n      spec: {\n        name: \"bash\",\n        description: \"Run a shell command in the current workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: { command: { type: \"string\" } },\n          required: [\"command\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"bash\",\n        input2,\n        () => runBash(String(input2.command || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"read_file\",\n        description: \"Read file contents from the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            limit: { type: \"integer\" }\n          },\n          required: [\"path\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"read_file\",\n        input2,\n        () => readWorkspaceFile(String(input2.path || \"\"), typeof input2.limit === \"number\" ? input2.limit : void 0),\n        {\n          ...options,\n          toolUseId,\n          onReadPath: (filePath) => options?.compact?.trackRecentFile(filePath)\n        }\n      )\n    },\n    {\n      spec: {\n        name: \"write_file\",\n        description: \"Write a file in the workspace.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            content: { type: \"string\" }\n          },\n          required: [\"path\", \"content\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"write_file\",\n        input2,\n        () => writeWorkspaceFile(String(input2.path || \"\"), String(input2.content || \"\")),\n        { ...options, toolUseId }\n      )\n    },\n    {\n      spec: {\n        name: \"edit_file\",\n        description: \"Replace exact text in a file once.\",\n        input_schema: {\n          type: \"object\",\n          properties: {\n            path: { type: \"string\" },\n            old_text: { type: \"string\" },\n            new_text: { type: \"string\" }\n          },\n          required: [\"path\", \"old_text\", \"new_text\"]\n        }\n      },\n      execute: (input2, _ctx, toolUseId) => withGuards(\n        \"edit_file\",\n        input2,\n        () => editWorkspaceFile(\n          String(input2.path || \"\"),\n          String(input2.old_text || \"\"),\n          String(input2.new_text || \"\")\n        ),\n        { ...options, toolUseId }\n      )\n    }\n  ];\n}\nvar PluginLoader = class {\n  pluginDir;\n  manifests = /* @__PURE__ */ new Map();\n  constructor(pluginDir = path.join(WORKDIR, \".plugins\")) {\n    this.pluginDir = pluginDir;\n    this.load();\n  }\n  load() {\n    this.manifests.clear();\n    if (!fs.existsSync(this.pluginDir)) {\n      return;\n    }\n    for (const fileName of fs.readdirSync(this.pluginDir)) {\n      if (!fileName.endsWith(\".json\")) {\n        continue;\n      }\n      const parsed = JSON.parse(fs.readFileSync(path.join(this.pluginDir, fileName), \"utf8\"));\n      this.manifests.set(parsed.name, parsed);\n    }\n  }\n  list() {\n    if (this.manifests.size === 0) {\n      return \"No plugins configured.\";\n    }\n    return [...this.manifests.values()].map((manifest) => `- ${manifest.name}: ${manifest.description || manifest.command}`).join(\"\\n\");\n  }\n  describe(name) {\n    const manifest = this.manifests.get(name);\n    if (!manifest) {\n      return `Error: Unknown plugin ${name}`;\n    }\n    return JSON.stringify(manifest, null, 2);\n  }\n};\nfunction createPluginTools(pluginLoader2) {\n  return [\n    {\n      spec: {\n        name: \"list_plugins\",\n        description: \"List available plugin manifests.\",\n        input_schema: { type: \"object\", properties: {} }\n      },\n      execute: () => pluginLoader2.list()\n    },\n    {\n      spec: {\n        name: \"describe_plugin\",\n        description: \"Read one plugin manifest.\",\n        input_schema: {\n          type: \"object\",\n          properties: { name: { type: \"string\" } },\n          required: [\"name\"]\n        }\n      },\n      execute: (input2) => pluginLoader2.describe(String(input2.name || \"\"))\n    }\n  ];\n}\n\n// agents_self_contained/s19_mcp_plugin.ts\nvar ctx = createLoopContext();\nvar pluginLoader = new PluginLoader();\nvar tools = [...createBaseTools(), ...createPluginTools(pluginLoader)];\nawait runRepl({\n  label: \"s19\",\n  ctx,\n  run: async (loopCtx) => {\n    pluginLoader.load();\n    await runAgentLoop(loopCtx, {\n      systemPrompt: `You are a coding agent at ${WORKDIR}. External tools should enter the same tool pipeline as native tools, even when they come from plugin manifests.`,\n      tools,\n      normalizeMessages: true\n    });\n  }\n});\n"
    }
  ],
  "diffs": [
    {
      "from": "s01",
      "to": "s02",
      "newClasses": [],
      "newFunctions": [],
      "newTools": [],
      "locDelta": 0
    },
    {
      "from": "s02",
      "to": "s03",
      "newClasses": [],
      "newFunctions": [
        "createTodoTool"
      ],
      "newTools": [
        "todo"
      ],
      "locDelta": 91
    },
    {
      "from": "s03",
      "to": "s04",
      "newClasses": [],
      "newFunctions": [
        "createTaskTool",
        "createSubagentRunner"
      ],
      "newTools": [
        "task"
      ],
      "locDelta": -54
    },
    {
      "from": "s04",
      "to": "s05",
      "newClasses": [],
      "newFunctions": [
        "parseFrontmatter",
        "walkFiles",
        "createLoadSkillTool"
      ],
      "newTools": [
        "load_skill"
      ],
      "locDelta": 51
    },
    {
      "from": "s05",
      "to": "s06",
      "newClasses": [],
      "newFunctions": [],
      "newTools": [],
      "locDelta": 12
    },
    {
      "from": "s06",
      "to": "s07",
      "newClasses": [],
      "newFunctions": [],
      "newTools": [],
      "locDelta": -35
    },
    {
      "from": "s07",
      "to": "s08",
      "newClasses": [],
      "newFunctions": [],
      "newTools": [],
      "locDelta": -3
    },
    {
      "from": "s08",
      "to": "s09",
      "newClasses": [],
      "newFunctions": [
        "parseFrontmatter",
        "createSaveMemoryTool"
      ],
      "newTools": [
        "save_memory"
      ],
      "locDelta": 63
    },
    {
      "from": "s09",
      "to": "s10",
      "newClasses": [],
      "newFunctions": [
        "walkFiles",
        "createLoadSkillTool",
        "buildPromptSections"
      ],
      "newTools": [
        "load_skill"
      ],
      "locDelta": 77
    },
    {
      "from": "s10",
      "to": "s11",
      "newClasses": [],
      "newFunctions": [],
      "newTools": [],
      "locDelta": -191
    },
    {
      "from": "s11",
      "to": "s12",
      "newClasses": [],
      "newFunctions": [
        "createTaskBoardTools"
      ],
      "newTools": [
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task"
      ],
      "locDelta": 114
    },
    {
      "from": "s12",
      "to": "s13",
      "newClasses": [],
      "newFunctions": [
        "createBackgroundTools"
      ],
      "newTools": [
        "background_run",
        "check_background"
      ],
      "locDelta": -9
    },
    {
      "from": "s13",
      "to": "s14",
      "newClasses": [],
      "newFunctions": [
        "createTaskBoardTools",
        "createCronTools"
      ],
      "newTools": [
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "schedule_task",
        "list_schedules",
        "run_due_schedules"
      ],
      "locDelta": 94
    },
    {
      "from": "s14",
      "to": "s15",
      "newClasses": [],
      "newFunctions": [
        "createTeamTools"
      ],
      "newTools": [
        "register_teammate",
        "list_teammates",
        "send_team_message",
        "read_team_inbox"
      ],
      "locDelta": -94
    },
    {
      "from": "s15",
      "to": "s16",
      "newClasses": [],
      "newFunctions": [
        "createProtocolTools"
      ],
      "newTools": [
        "request_plan_approval",
        "respond_to_request"
      ],
      "locDelta": 48
    },
    {
      "from": "s16",
      "to": "s17",
      "newClasses": [],
      "newFunctions": [
        "createTaskBoardTools"
      ],
      "newTools": [
        "create_task_record",
        "list_task_records",
        "update_task_record",
        "claim_unclaimed_task",
        "scan_unclaimed_tasks"
      ],
      "locDelta": -25
    },
    {
      "from": "s17",
      "to": "s18",
      "newClasses": [],
      "newFunctions": [
        "createWorktreeTools"
      ],
      "newTools": [
        "create_lane",
        "list_lanes",
        "run_in_lane"
      ],
      "locDelta": 49
    },
    {
      "from": "s18",
      "to": "s19",
      "newClasses": [],
      "newFunctions": [
        "createPluginTools"
      ],
      "newTools": [
        "list_plugins",
        "describe_plugin"
      ],
      "locDelta": -128
    }
  ]
}